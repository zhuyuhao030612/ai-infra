[project]
name = "rust-review-scripts"
version = "0.1.0"
description = "Build-plan + SARIF helpers for the rust-review plugin"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100

[tool.ty.rules]
