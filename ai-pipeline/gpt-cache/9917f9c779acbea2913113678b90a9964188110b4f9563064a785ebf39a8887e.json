{
  "timestamp": "2026-06-29T19:40:47.8066775+08:00",
  "response": "PowerShell# =====================================================================# 新建: D:\\Code\\ai-infra\\scripts\\pipeline-fallback.ps1# 追加 health.ps1 检查项见本代码块底部# =====================================================================# D:\\Code\\ai-infra\\scripts\\pipeline-fallback.ps1Set-StrictMode -Version 2.0$ErrorActionPreference = \"Continue\"function Get-ProjectRoot { if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { return (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } $current = $PSScriptRoot for ($i = 0; $i -lt 8; $i++) { if (-not $current) { break } if ( (Test-Path -LiteralPath (Join-Path $current \"CLAUDE.md\")) -or (Test-Path -LiteralPath (Join-Path $current \"CAPABILITY_REGISTRY.md\")) -or (Test-Path -LiteralPath (Join-Path $current \".claude\\run-state\\run-state.lib.ps1\")) ) { return $current } $parent = Split-Path -Parent $current if (-not $parent -or $parent -eq $current) { break } $current = $parent } return (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot \"..\\..\")).Path}function Save-PipelineHealth { param( [Parameter(Mandatory = $true)] [object]$Health ) try { $stateDir = Join-Path $script:Root \".claude\\run-state\" if (-not (Test-Path -LiteralPath $stateDir)) { New-Item -ItemType Directory -Path $stateDir -Force | Out-Null } $healthPath = Join-Path $stateDir \"pipeline-health.json\" $Health | ConvertTo-Json -Depth 20 | Set-Content -LiteralPath $healthPath -Encoding UTF8 } catch { # fail-open: 健康状态写入失败不影响降级判断输出 return }}function Test-QueueReadWrite { param([string]$QueueDir) try { if (-not (Test-Path -LiteralPath $QueueDir)) { New-Item -ItemType Directory -Path $QueueDir -Force | Out-Null } $probePath = Join-Path $QueueDir (\"fallback-probe-{0}.tmp\" -f ([guid]::NewGuid().ToString(\"N\"))) \"ok\" | Set-Content -LiteralPath $probePath -Encoding UTF8 $value = Get-Content -LiteralPath $probePath -Raw -Encoding UTF8 Remove-Item -LiteralPath $probePath -Force -ErrorAction SilentlyContinue return ($value.Trim() -eq \"ok\") } catch { return $false }}function Test-RecentGptResponse { param( [string]$OutboxDir, [int]$Minutes = 5 ) try { if (-not (Test-Path -LiteralPath $OutboxDir)) { return $false } $cutoff = (Get-Date).AddMinutes(-1 * $Minutes) $recent = @(Get-ChildItem -LiteralPath $OutboxDir -File -ErrorAction SilentlyContinue | Where-Object { $_.Name -like \"*.response.json\" -and $_.LastWriteTime -ge $cutoff }) return ($recent.Count -gt 0) } catch { return $false }}function Test-Gpt55Pipeline { param([string]$Root) $pipelineDir = Join-Path $Root \"ai-pipeline\" $queueDir = Join-Path $pipelineDir \"gpt-queue\" $outboxDir = Join-Path $queueDir \"outbox\" $queueOk = Test-QueueReadWrite -QueueDir $queueDir $recentResponseOk = Test-RecentGptResponse -OutboxDir $outboxDir -Minutes 5 return [pscustomobject]@{ available = ($queueOk -and $recentResponseOk) queue_read_write = $queueOk recent_response_5m = $recentResponseOk queue_dir = $queueDir outbox_dir = $outboxDir }}function Test-GlmPipeline { param([string]$Root) try { $glmAsk = Join-Path $Root \"ai-infra\\scripts\\glm-ask.ps1\" if (-not (Test-Path -LiteralPath $glmAsk)) { $glmAsk = Join-Path $Root \"glm-ask.ps1\" } if (-not (Test-Path -LiteralPath $glmAsk)) { return [pscustomobject]@{ available = $false path = $glmAsk reason = \"glm-ask.ps1 not found\" } } return [pscustomobject]@{ available = $true path = $glmAsk reason = \"glm-ask.ps1 exists\" } } catch { return [pscustomobject]@{ available = $false path = \"\" reason = $_.Exception.Message } }}function Test-OllamaQwen { try { $resp = Invoke-RestMethod -Uri \"http://127.0.0.1:11434/api/tags\" -Method Get -TimeoutSec 3 $models = @() if ($resp -and $resp.models) { $models = @($resp.models | ForEach-Object { [string]$_.name }) } $hasQwen = [bool](@($models | Where-Object { $_ -match \"qwen2\\.5-coder:7b|qwen2\\.5-coder\" }).Count -gt 0) return [pscustomobject]@{ available = $hasQwen endpoint = \"http://127.0.0.1:11434/api/tags\" models = $models reason = if ($hasQwen) { \"qwen2.5-coder available\" } else { \"qwen2.5-coder not found\" } } } catch { return [pscustomobject]@{ available = $false endpoint = \"http://127.0.0.1:11434/api/tags\" models = @() reason = $_.Exception.Message } }}$script:Root = Get-ProjectRoot$env:CLAUDE_PROJECT_DIR = $script:Root$timestamp = (Get-Date).ToString(\"s\")$gpt = Test-Gpt55Pipeline -Root $script:Root$glm = Test-GlmPipeline -Root $script:Root$ollama = Test-OllamaQwen$fallbackLevel = \"none\"$primaryAvailable = $false$selected = \"none\"$exitCode = 0$message = \"\"if ($gpt.available) { $primaryAvailable = $true $fallbackLevel = \"none\" $selected = \"GPT-5.5\" $message = \"GPT-5.5可用\" Write-Host $message $exitCode = 0}elseif ($glm.available) { $fallbackLevel = \"glm\" $selected = \"GLM-5.2\" $message = \"GPT-5.5不可用，降级到GLM-5.2\" Write-Host $message $exitCode = 0}elseif ($ollama.available) { $fallbackLevel = \"ollama\" $selected = \"Ollama qwen2.5-coder\" $message = \"GPT-5.5和GLM不可用，降级到Ollama qwen2.5-coder\" Write-Host $message $exitCode = 0}else { $fallbackLevel = \"none-available\" $selected = \"none\" $message = \"所有外脑不可用，停止自驱\" Write-Host $message $exitCode = 1}$health = [pscustomobject]@{ timestamp = $timestamp primary = \"GPT-5.5\" primary_available = $primaryAvailable selected = $selected fallback_level = $fallbackLevel message = $message gpt55 = $gpt glm = $glm ollama = $ollama}Save-PipelineHealth -Health $healthexit $exitCode# =====================================================================# D:\\Code\\health.ps1 追加检查函数与集成# 位置: 插入到函数区；在 $checks 构建处调用 $checks.Add((Test-PipelineFallback))# =====================================================================function Test-PipelineFallback { Set-StrictMode -Version 2.0 $ErrorActionPreference = \"Continue\" try { $scriptPath = Join-Path $root \"ai-infra\\scripts\\pipeline-fallback.ps1\" if (-not (Test-Path -LiteralPath $scriptPath)) { return (Check \"pipeline_fallback\" $false \"FAIL missing pipeline-fallback.ps1\") } $output = @(& pwsh -NoProfile -ExecutionPolicy Bypass -File $scriptPath 2>&1 | ForEach-Object { [string]$_ }) $exitCode = $LASTEXITCODE if ($null -eq $exitCode) { $exitCode = 0 } $healthPath = Join-Path $root \".claude\\run-state\\pipeline-health.json\" $detail = (($output | Select-Object -Last 5) -join \" \") -replace \"\\s+\", \" \" if ($exitCode -eq 0) { if (Test-Path -LiteralPath $healthPath) { try { $health = Get-Content -LiteralPath $healthPath -Raw -Encoding UTF8 | ConvertFrom-Json return (Check \"pipeline_fallback\" $true (\"OK selected={0} fallback={1} primary={2}; {3}\" -f $health.selected, $health.fallback_level, $health.primary_available, $detail)) } catch { return (Check \"pipeline_fallback\" $true (\"OK but health json parse warning: \" + $_.Exception.Message)) } } return (Check \"pipeline_fallback\" $true (\"OK \" + $detail)) } return (Check \"pipeline_fallback\" $false (\"FAIL \" + $detail)) } catch { return (Check \"pipeline_fallback\" $true (\"WARN fail-open: \" + $_.Exception.Message)) }}# 集成:# $checks.Add((Test-PipelineFallback))\nPowerShell# =====================================================================# 新建: D:\\Code\\ai-infra\\scripts\\capability-usage-report.ps1# 追加 posttool-recorder.ps1 集成片段见本代码块底部# =====================================================================# D:\\Code\\ai-infra\\scripts\\capability-usage-report.ps1Set-StrictMode -Version 2.0$ErrorActionPreference = \"Continue\"param( [string]$MarkUsage = \"\", [int]$ExitCode = 0, [int]$DurationMs = 0)function Get-ProjectRoot { if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { return (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } $current = $PSScriptRoot for ($i = 0; $i -lt 8; $i++) { if (-not $current) { break } if ( (Test-Path -LiteralPath (Join-Path $current \"CLAUDE.md\")) -or (Test-Path -LiteralPath (Join-Path $current \"CAPABILITY_REGISTRY.md\")) -or (Test-Path -LiteralPath (Join-Path $current \".claude\\run-state\\run-state.lib.ps1\")) ) { return $current } $parent = Split-Path -Parent $current if (-not $parent -or $parent -eq $current) { break } $current = $parent } return (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot \"..\\..\")).Path}function Get-UsagePath { param([string]$Root) $stateDir = Join-Path $Root \".claude\\run-state\" if (-not (Test-Path -LiteralPath $stateDir)) { New-Item -ItemType Directory -Path $stateDir -Force | Out-Null } return (Join-Path $stateDir \"capability-usage.jsonl\")}function Write-Usage { param( [Parameter(Mandatory = $true)][string]$Path, [Parameter(Mandatory = $true)][string]$ScriptName, [int]$ExitCode, [int]$DurationMs ) try { if (-not (Test-Path -LiteralPath $Path)) { New-Item -ItemType File -Path $Path -Force | Out-Null } $record = [ordered]@{ time = (Get-Date).ToString(\"s\") script = $ScriptName capability = [System.IO.Path]::GetFileNameWithoutExtension($ScriptName) exit_code = $ExitCode duration_ms = $DurationMs success = ($ExitCode -eq 0) } ($record | ConvertTo-Json -Depth 10 -Compress) | Add-Content -LiteralPath $Path -Encoding UTF8 } catch { # fail-open return }}function Read-UsageRecords { param([string]$Path) $records = @() try { if (-not (Test-Path -LiteralPath $Path)) { New-Item -ItemType File -Path $Path -Force | Out-Null return @() } $lines = @(Get-Content -LiteralPath $Path -Encoding UTF8 -ErrorAction SilentlyContinue) foreach ($line in $lines) { if ([string]::IsNullOrWhiteSpace($line)) { continue } try { $records += ($line | ConvertFrom-Json) } catch { continue } } } catch { return @() } return $records}function Get-UsageSummary { param([object[]]$Records) $now = Get-Date $groups = @($Records | Group-Object -Property script) $summary = @() foreach ($group in $groups) { $items = @($group.Group) $count = $items.Count $successCount = @($items | Where-Object { $_.success -eq $true }).Count $successRate = if ($count -gt 0) { [math]::Round(($successCount / $count) * 100, 1) } else { 0 } $last = @($items | Sort-Object { [datetime]$_.time } -Descending | Select-Object -First 1) $lastTime = $null if ($last.Count -gt 0) { try { $lastTime = [datetime]$last[0].time } catch { $lastTime = $null } } $avgDuration = 0 $durations = @($items | Where-Object { $null -ne $_.duration_ms } | ForEach-Object { [int]$_.duration_ms }) if ($durations.Count -gt 0) { $avgDuration = [math]::Round((($durations | Measure-Object -Average).Average), 0) } $ageDays = 9999 if ($null -ne $lastTime) { $ageDays = [math]::Round(($now - $lastTime).TotalDays, 1) } $bucket = \"休眠\" $color = \"Red\" if ($ageDays -le 7) { $bucket = \"活跃\" $color = \"Green\" } elseif ($ageDays -ge 60) { $bucket = \"休眠\" $color = \"Red\" } elseif ($ageDays -ge 30) { $bucket = \"低频\" $color = \"Yellow\" } else { $bucket = \"观察\" $color = \"DarkYellow\" } $summary += [pscustomobject]@{ script = $group.Name calls = $count success_rate = $successRate last_called_at = if ($null -ne $lastTime) { $lastTime.ToString(\"s\") } else { \"\" } age_days = $ageDays avg_duration_ms = $avgDuration bucket = $bucket color = $color } } return @($summary | Sort-Object bucket, script)}function Write-Section { param( [string]$Title, [object[]]$Items, [string]$Color ) Write-Host \"\" Write-Host $Title -ForegroundColor $Color if ($Items.Count -eq 0) { Write-Host \" none\" return } foreach ($item in $Items) { Write-Host (\" {0} calls={1} success={2}% last={3} avg={4}ms age={5}d\" -f ` $item.script, $item.calls, $item.success_rate, $item.last_called_at, $item.avg_duration_ms, $item.age_days ) -ForegroundColor $Color }}$root = Get-ProjectRoot$env:CLAUDE_PROJECT_DIR = $root$usagePath = Get-UsagePath -Root $rootif (-not [string]::IsNullOrWhiteSpace($MarkUsage)) { Write-Usage -Path $usagePath -ScriptName $MarkUsage -ExitCode $ExitCode -DurationMs $DurationMs Write-Host (\"usage recorded: {0} exit={1} duration={2}ms\" -f $MarkUsage, $ExitCode, $DurationMs) exit 0}$records = @(Read-UsageRecords -Path $usagePath)$summary = @(Get-UsageSummary -Records $records)Write-Host (\"Capability usage report: {0}\" -f $usagePath)Write-Host (\"records={0} scripts={1}\" -f $records.Count, $summary.Count)$active = @($summary | Where-Object { $_.bucket -eq \"活跃\" })$low = @($summary | Where-Object { $_.bucket -eq \"低频\" })$sleeping = @($summary | Where-Object { $_.bucket -eq \"休眠\" })$watch = @($summary | Where-Object { $_.bucket -eq \"观察\" })Write-Section -Title \"活跃(7天内调用过)\" -Items $active -Color \"Green\"Write-Section -Title \"观察(8-29天未调用)\" -Items $watch -Color \"DarkYellow\"Write-Section -Title \"低频(30天未调用)\" -Items $low -Color \"Yellow\"Write-Section -Title \"休眠(60天+未调用，建议废弃)\" -Items $sleeping -Color \"Red\"$reportPath = Join-Path $root \".claude\\run-state\\capability-usage-report.json\"[pscustomobject]@{ generated_at = (Get-Date).ToString(\"s\") usage_path = $usagePath total_records = $records.Count total_scripts = $summary.Count active = $active watch = $watch low_frequency = $low sleeping = $sleeping summary = $summary} | ConvertTo-Json -Depth 30 | Set-Content -LiteralPath $reportPath -Encoding UTF8Write-Host \"\"Write-Host (\"report saved: {0}\" -f $reportPath)exit 0# =====================================================================# D:\\Code\\.claude\\hooks\\posttool-recorder.ps1 追加集成片段# 位置: 已解析出 $toolName / $toolInput / $commandText 后；或检测脚本执行的分支里调用# =====================================================================function Add-CapabilityUsageRecord { param( [Parameter(Mandatory = $true)] [string]$ScriptName, [int]$ExitCode = 0, [int]$DurationMs = 0 ) try { $projectRoot = $env:CLAUDE_PROJECT_DIR if (-not $projectRoot) { $projectRoot = Split-Path -Parent (Split-Path -Parent $PSScriptRoot) } $usageScript = Join-Path $projectRoot \"ai-infra\\scripts\\capability-usage-report.ps1\" if (-not (Test-Path -LiteralPath $usageScript)) { return } & pwsh -NoProfile -ExecutionPolicy Bypass -File $usageScript -MarkUsage $ScriptName -ExitCode $ExitCode -DurationMs $DurationMs | Out-Null } catch { # fail-open: usage 写入失败不影响 recorder return }}function Get-ExecutedScriptFromCommand { param([string]$CommandText) try { if ([string]::IsNullOrWhiteSpace($CommandText)) { return \"\" } $patterns = @( \"([A-Za-z]:\\\\[^`\"'\\s]+\\.ps1)\", \"([A-Za-z]:\\\\[^`\"'\\s]+\\.py)\", \"([A-Za-z]:\\\\[^`\"'\\s]+\\.js)\", \"([A-Za-z]:\\\\[^`\"'\\s]+\\.bat)\", \"([^`\"'\\s]+\\.ps1)\", \"([^`\"'\\s]+\\.py)\", \"([^`\"'\\s]+\\.js)\", \"([^`\"'\\s]+\\.bat)\" ) foreach ($pattern in $patterns) { $m = [regex]::Match($CommandText, $pattern) if ($m.Success) { return ([System.IO.Path]::GetFileName($m.Groups[1].Value)) } } return \"\" } catch { return \"\" }}# integration:# $commandText 应使用 posttool-recorder 已有的命令文本变量；# 如果当前变量名不同，把下面 $commandText 替换为现有变量。## $executedScript = Get-ExecutedScriptFromCommand -CommandText $commandText# if (-not [string]::IsNullOrWhiteSpace($executedScript)) {# $exitCodeForUsage = 0# if ($null -ne $LASTEXITCODE) { $exitCodeForUsage = [int]$LASTEXITCODE }# Add-CapabilityUsageRecord -ScriptName $executedScript -ExitCode $exitCodeForUsage -DurationMs 0# }"
}
