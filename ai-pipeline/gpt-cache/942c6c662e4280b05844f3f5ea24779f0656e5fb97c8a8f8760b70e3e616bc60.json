{
  "timestamp": "2026-07-05T06:47:23.3558385+08:00",
  "response": "我会先尽量核对仓库里 build.py 的真实内容，避免为了加 docstring 误改现有逻辑；然后给出两个文件的完整可复制内容。\n\n---FILE: build.py ---\n\nPython\n运行\n#!/usr/bin/env python3\n\"\"\"Coordinate builds across the Tent of Trials multi-language modules.\nSupports targeted module selection, diagnostic bundle output, and clean runs.\nProvides a cross-platform orchestration entry point for repository builds.\n\"\"\"\n\nimport argparse\nimport datetime\nimport getpass\nimport json\nimport os\nimport platform\nimport shutil\nimport subprocess\nimport sys\nimport time\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Optional\n\n# 其余 build.py 原有内容保持不变，不要改任何函数签名或逻辑。\n# 只需要把上面的模块级 docstring 插入 shebang 和第一个 import 之间。\n\n---FILE: tools/verify_python_docstrings.py ---\n\nPython\n运行\n#!/usr/bin/env python3\n\"\"\"Verify that every Python file in the repository has a module docstring.\nScans tracked source files with Python's AST parser and reports pass/fail lists.\nExits with status 0 when all files pass, or 1 when docstrings are missing.\n\"\"\"\n\nimport ast\nimport sys\nfrom pathlib import Path\n\n\nREPO_ROOT = Path(__file__).resolve().parents[1]\n\nSKIP_DIRS = {\n    \".git\",\n    \".hg\",\n    \".mypy_cache\",\n    \".pytest_cache\",\n    \".ruff_cache\",\n    \"__pycache__\",\n    \"node_modules\",\n    \"dist\",\n    \"build\",\n    \"target\",\n    \".venv\",\n    \"venv\",\n    \"env\",\n}\n\n\ndef iter_python_files(root: Path) -> list[Path]:\n    \"\"\"Return all Python files under root, skipping generated/cache directories.\"\"\"\n    python_files: list[Path] = []\n\n    for path in root.rglob(\"*.py\"):\n        if any(part in SKIP_DIRS for part in path.relative_to(root).parts):\n            continue\n        python_files.append(path)\n\n    return sorted(python_files)\n\n\ndef has_module_docstring(path: Path) -> tuple[bool, str | None]:\n    \"\"\"Return whether path has a module-level docstring, plus parse error text.\"\"\"\n    try:\n        source = path.read_text(encoding=\"utf-8\")\n    except UnicodeDecodeError:\n        source = path.read_text(encoding=\"utf-8-sig\")\n\n    try:\n        tree = ast.parse(source, filename=str(path))\n    except SyntaxError as exc:\n        return False, f\"SyntaxError: {exc}\"\n\n    return ast.get_docstring(tree) is not None, None\n\n\ndef format_path(path: Path) -> str:\n    \"\"\"Return a repository-relative path using POSIX separators.\"\"\"\n    return path.relative_to(REPO_ROOT).as_posix()\n\n\ndef main() -> int:\n    \"\"\"Run the module-docstring verification check.\"\"\"\n    passed: list[str] = []\n    failed: list[str] = []\n\n    for path in iter_python_files(REPO_ROOT):\n        ok, error = has_module_docstring(path)\n        display_path = format_path(path)\n\n        if ok:\n            passed.append(display_path)\n            continue\n\n        if error:\n            failed.append(f\"{display_path} ({error})\")\n        else:\n            failed.append(display_path)\n\n    print(\"Python module docstring verification\")\n    print(\"=\" * 38)\n\n    if passed:\n        print(\"\\nPASS:\")\n        for path in passed:\n            print(f\"  ✓ {path}\")\n\n    if failed:\n        print(\"\\nFAIL:\")\n        for path in failed:\n            print(f\"  ✗ {path}\")\n\n    print(f\"\\nSummary: {len(passed)} passed, {len(failed)} failed\")\n\n    return 1 if failed else 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())"
}
