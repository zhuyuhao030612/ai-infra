{
  "timestamp": "2026-06-29T21:08:53.2872735+08:00",
  "response": "ps-guard.ps1\n行号问题类型描述修复代码20-21 / 34-38bugC 盘路径规则会误伤 ps-guard 自己的正则字符串，例如 '\"C:\\\\[^\"]*\"' 本身会被识别成硬编码 C 路径，导致自检或检查含路径正则的脚本时误 FAIL。<pre><code># 从 $rules 中删除这两条 raw-content C 路径规则：\n@{p='\"C:\\[^\"]*\"';s='E';m='Hardcoded C: path is not allowed'},\n@{p=\"''C:\\[^'']*''\";s='E';m='Hardcoded C: path is not allowed'},\n替换 String/path scan 中的 C 路径判断：\nif($tokens){\n$tokens|Where-Object{$.Kind -in @('StringExpandable','StringLiteral')}|ForEach-Object{\n$t=$.Text\nif($t -match '(?i)(Bearer\\s+[A-Za-z0-9.~+/=-]+|sk-[A-Za-z0-9-]+|ghp_[A-Za-z0-9_]+)'){Err 'Possible credential literal'}\n# 避免把正则模式自身当成硬编码 C 路径$looksLikeRegexPattern = ($t -match '\\\\\\\\\\[\\^|\\\\\\[\\^|\\.\\*|\\(\\?i\\)')if(-not $looksLikeRegexPattern -and $t -match '(?i)(^|[\"''])C:\\\\[^\"'']+'){ Err \"C: path-like string: $($t.Substring(0,[Math]::Min(80,$t.Length)))\"}\n}\n}</code></pre> |\n\nCLAUDE.md\n行号问题类型描述修复代码24protocol文档仍写“本轮第一个工具调用必须 Grep CAPABILITY_REGISTRY.md”，但当前体系已把 A5 改为非阻塞提醒，并由 ai-start 设置 capability_checked=true。继续保留“首调强制 Grep”会让 DeepSeek 误执行旧协议，甚至复发 A5 自锁。<pre><code>## 能力注册表—由 ai-start 初始化\n每轮入口必须先跑 ai-start.ps1。ai-start 负责读取/确认 CAPABILITY_REGISTRY.md，写入 current.json 的 task_started=true 和 capability_checked=true。\n复杂、模糊、高风险任务可再运行 agent-self-check.ps1 或 Grep \"关键词\" CAPABILITY_REGISTRY.md 辅助路由；但不再要求“第一个工具调用必须 Grep”。\n修改走 Hook。结束前 evidence-check→close-run。</code></pre> |\n\nagent-self-check.ps1\n行号问题类型描述修复代码15protocol默认 RegistryPath 硬编码到 C:\\Users\\...，违反“禁止硬编码 C 盘”协议，也会导致项目迁移或不同用户环境下能力注册表找不到。<pre><code>param(\n[Parameter(Mandatory=$true)][string]$Task,[string]$RegistryPath = \"\",[int]$Top = 8,[switch]$Json\n)\n$ProjectRoot = if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) {\n(Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path\n} else {\n\"D:\\Code\"\n}\nif ([string]::IsNullOrWhiteSpace($RegistryPath)) {\n$RegistryPath = Join-Path $ProjectRoot \"CAPABILITY_REGISTRY.md\"\n}</code></pre> |\n| 51 | bug | return if (...) { ... } else { ... } 不是合法 PowerShell 语法，会导致脚本解析失败。 | <pre><code>function Jaccard {\nparam([System.Collections.Generic.HashSet[string]]$A, [System.Collections.Generic.HashSet[string]]$B)\nif ($A.Count -eq 0 -or $B.Count -eq 0) { return 0.0}$inter = 0foreach ($x in $A) { if ($B.Contains($x)) { $inter++ }}$union = $A.Count + $B.Count - $interif ($union -le 0) { return 0.0}return ([double]$inter / [double]$union)\n}</code></pre> |"
}
