{
  "timestamp": "2026-06-29T21:23:13.9325587+08:00",
  "response": "gpt-ask.ps1\nCLEAN\n\nglm-ask.ps1\n行号问题类型描述修复代码72-74bugSet-StrictMode -Version Latest 下直接访问 $result.choices[0].message.reasoning_content。如果 API 响应没有 reasoning_content 字段，会抛异常，导致正常非 Thinking 响应被判为 GLM_UNAVAILABLE。<pre><code>$message = $result.choices[0].message$reasoning = \"\"\nif ($message.PSObject.Properties.Name -contains \"reasoning_content\" -and $message.reasoning_content) {\n$reasoning = [string]$message.reasoning_content\n}</code></pre> |\n| 36-38 / 76-79 | bug | 直接访问 $usage.prompt_tokens / $usage.completion_tokens。如果 API 返回缺少 usage 或字段名变化，正常内容响应会因为计费统计失败而整体失败。 | <pre><code>$usage = $result.usage\n$promptTokens = 0\n$completionTokens = 0\nif ($null -ne $usage) {\nif ($usage.PSObject.Properties.Name -contains \"prompt_tokens\") {\n$promptTokens = [int]$usage.prompt_tokens\n}\nif ($usage.PSObject.Properties.Name -contains \"completion_tokens\") {\n$completionTokens = [int]$usage.completion_tokens\n}\n}\n$costIn = ($promptTokens / 1000000) * 1.40\n$costOut = ($completionTokens / 1000000) * 4.40\n$costTotal = [math]::Round($costIn + $costOut, 4)</code></pre> |\n\nlearn.ps1\n行号问题类型描述修复代码18-20protocol硬编码 C:\\Users\\... 路径，违反当前项目“D盘为主、禁止硬编码C盘、从项目根/环境推导”的协议。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$OllamaBin = if ($env:OLLAMA_BIN) {\n$env:OLLAMA_BIN\n} else {\n\"ollama\"\n}\n$MemDir = Join-Path $RootDir \"memory\"</code></pre> |\n| 43-47 | protocol | GPT-5.5 审核结果只打印，没有用于存档。实际存档仍是未经 GPT 审核的 $ollamaJson，违反“Ollama 预处理 → GPT-5.5 审核 → 存档”的管道协议。 | <pre><code>$finalJson = $ollamaJson\nif (-not $NoGPT) {\nWrite-Host \"`n=== GPT-5.5 审核... ===\" -ForegroundColor Cyan\n$gptPrompt = \"审核并优化以下知识点。1)删除不准确的 2)合并重复的 3)补充缺失的 4)修正分类错误。返回优化后的JSON。原始提取: $ollamaJson\"\n$gptAsk = Join-Path $RootDir \"ai-infra\\scripts\\gpt-ask.ps1\"\n$gptResult = & pwsh -NoLogo -File $gptAsk -Prompt $gptPrompt -TimeoutSec $TimeoutSec 2>&1\nif ($LASTEXITCODE -eq 0 -and -not [string]::IsNullOrWhiteSpace(($gptResult -join \"`n\"))) { $finalJson = ($gptResult -join \"`n\")} else { Write-Host \"WARN: GPT审核失败，保留Ollama结果\"}\n}\n后续存档中把 $ollamaJson 替换为 $finalJson</code></pre> |\n| 43 | protocol | GPT 审核只在 -Search 时执行；-Text 输入默认绕过 GPT，除非用户传 -NoGPT。这和脚本声明的质量门协议不一致。 | <pre><code># 替换：\nif (-not $NoGPT -and $Search) {\n改为：\nif (-not $NoGPT) {</code></pre> |\n| 59 | bug | 写入 $MemDir 前没有确保目录存在。目录不存在时存档失败。 | <pre><code>if (-not (Test-Path -LiteralPath $MemDir)) {\nNew-Item -ItemType Directory -Force -Path $MemDir | Out-Null\n}\n$file = Join-Path $MemDir \"$slug.md\"</code></pre> |\n\nnew-failure.ps1\n行号问题类型描述修复代码11 / 13protocol$FailuresDir = \"D:\\Code\\...\" 硬编码项目根，不跟随 CLAUDE_PROJECT_DIR / AI_ROOT，迁移或非 D:\\Code 环境会写错位置。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$FailuresDir = Join-Path $RootDir \"ai-infra\\failures\"</code></pre> |\n| 13 | bug | $ErrorType 未净化就拼进目录名。包含 :、\\、/、.. 等字符时可能创建失败或产生路径穿越。 | <pre><code>$safeErrorType = $ErrorType -replace '[\\/:*?\"<>|]', '-'\n$safeErrorType = $safeErrorType -replace '..', '-'\n$safeErrorType = $safeErrorType.Trim()\nif ([string]::IsNullOrWhiteSpace($safeErrorType)) {\n$safeErrorType = \"unknown\"\n}\n$runId = Get-Date -Format \"yyyyMMdd-HHmmss\"\n$runDir = Join-Path $FailuresDir \"$runId-$safeErrorType\"</code></pre> |\n| 53-62 | bug | Artifacts 的值同时被当作“manifest 文件名”和“内容”使用。传入内容时会被拼成路径；传入文件名时会把文件名写进文件，不能可靠生成 artifact。 | <pre><code># 约定 Artifacts[k] = 内容；文件名由 key 生成\n$textExtensions = @(\".txt\", \".md\", \".html\", \".json\", \".log\", \".js\", \".py\", \".ps1\", \".csv\")\nforeach ($k in $Artifacts.Keys) {\n$safeName = ([string]$k) -replace '[\\/:*?\"<>|]', '-'\nif ([string]::IsNullOrWhiteSpace($safeName)) { continue }\n$fileName = \"$safeName.txt\"$path = Join-Path $runDir $fileName$content = [string]$Artifacts[$k]$content = Redact-Text $content$content | Out-File $path -Encoding UTF8$manifestArtifacts[$k] = $fileName\n}\n注意：manifest 应在 artifact 循环后再写入，否则 artifacts 字段不会包含最终文件名。</code></pre> |\n| 66 | bug | 手拼 DataJson 未做 JSON 转义。$Source / $ErrorType 包含引号、反斜杠、换行时会生成非法 JSON。 | <pre><code>$logEvent = Join-Path $RootDir \"ai-infra\\scripts\\log-event.ps1\"\nif (Test-Path -LiteralPath $logEvent) {\n$dataJson = [ordered]@{\nerror_type = $ErrorType\nsource = $Source\nid = \"$runId-$safeErrorType\"\n} | ConvertTo-Json -Compress\n& pwsh -File $logEvent -Type \"failure.created\" -Ok:$false -DataJson $dataJson\n}</code></pre> |"
}
