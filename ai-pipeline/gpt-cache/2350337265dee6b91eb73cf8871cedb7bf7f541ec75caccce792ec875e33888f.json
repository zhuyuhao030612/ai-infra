{
  "timestamp": "2026-06-28T19:34:42.5999575+08:00",
  "response": "app/material_engine/engines/clean_detection.py\nPythonfrom __future__ import annotationsimport argparseimport jsonimport mathimport timeimport hashlibfrom dataclasses import dataclass, asdictfrom pathlib import Pathfrom typing import Anyimport cv2import numpy as npIMAGE_EXTS = {\".jpg\", \".jpeg\", \".png\", \".webp\", \".bmp\"}VIDEO_EXTS = {\".mp4\", \".mov\", \".mkv\", \".avi\", \".webm\", \".m4v\"}TARGET_W = 1080TARGET_H = 1920@dataclassclass CleanMask: mask_id: str asset_id: str type: str bbox: list[int] confidence: float recommended_action: str area_ratio: float frame_hits: int sampled_frames: int review_required: bool reason: strdef _now_ms() -> int: return int(time.time() * 1000)def _safe_mkdir(path: Path) -> None: path.mkdir(parents=True, exist_ok=True)def _json_dump(path: Path, data: dict[str, Any]) -> None: _safe_mkdir(path.parent) path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding=\"utf-8\")def _asset_id_from_path(path: Path) -> str: h = hashlib.sha1(str(path).encode(\"utf-8\", errors=\"ignore\")).hexdigest()[:10] return f\"asset_{h}\"def _norm_asset(asset: Any) -> dict[str, Any]: if isinstance(asset, (str, Path)): p = Path(asset) return {\"asset_id\": _asset_id_from_path(p), \"path\": str(p)} if isinstance(asset, dict): p = asset.get(\"path\") or asset.get(\"source_path\") or asset.get(\"file_path\") or asset.get(\"input\") if not p: raise ValueError(f\"asset 缺少 path/source_path/file_path/input: {asset}\") aid = asset.get(\"asset_id\") or asset.get(\"id\") or _asset_id_from_path(Path(p)) out = dict(asset) out[\"asset_id\"] = aid out[\"path\"] = str(p) return out raise TypeError(f\"不支持的 asset 类型: {type(asset)}\")def _get_assets_from_ctx(ctx: dict[str, Any]) -> list[dict[str, Any]]: raw = ( ctx.get(\"assets\") or ctx.get(\"asset_pool\") or ctx.get(\"input_assets\") or ctx.get(\"clean_assets\") or [] ) return [_norm_asset(a) for a in raw]def _read_image(path: Path) -> np.ndarray | None: data = np.fromfile(str(path), dtype=np.uint8) if data.size == 0: return None img = cv2.imdecode(data, cv2.IMREAD_COLOR) return imgdef _resize_for_detection(frame: np.ndarray) -> tuple[np.ndarray, float, float]: h, w = frame.shape[:2] if w <= 0 or h <= 0: return frame, 1.0, 1.0 scale = min(TARGET_W / w, TARGET_H / h, 1.0) nw = max(1, int(w * scale)) nh = max(1, int(h * scale)) if scale != 1.0: frame = cv2.resize(frame, (nw, nh), interpolation=cv2.INTER_AREA) return frame, w / nw, h / nhdef _sample_frames(path: Path, max_frames: int = 16) -> tuple[list[np.ndarray], dict[str, Any]]: ext = path.suffix.lower() meta: dict[str, Any] = {\"path\": str(path), \"type\": \"unknown\"} if ext in IMAGE_EXTS: img = _read_image(path) if img is None: return [], {\"path\": str(path), \"type\": \"image\", \"error\": \"read_image_failed\"} h, w = img.shape[:2] meta.update({\"type\": \"image\", \"width\": w, \"height\": h, \"duration_sec\": 0, \"fps\": 0}) return [img], meta if ext not in VIDEO_EXTS: return [], {\"path\": str(path), \"type\": \"unknown\", \"error\": f\"unsupported_ext:{ext}\"} cap = cv2.VideoCapture(str(path)) if not cap.isOpened(): return [], {\"path\": str(path), \"type\": \"video\", \"error\": \"video_open_failed\"} fps = float(cap.get(cv2.CAP_PROP_FPS) or 0) frame_count = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH) or 0) height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT) or 0) duration = frame_count / fps if fps > 0 else 0.0 meta.update( { \"type\": \"video\", \"width\": width, \"height\": height, \"fps\": fps, \"frame_count\": frame_count, \"duration_sec\": duration, } ) frames: list[np.ndarray] = [] if frame_count <= 0: ok, frame = cap.read() if ok and frame is not None: frames.append(frame) cap.release() return frames, meta n = min(max_frames, max(1, frame_count)) indices = np.linspace(0, max(0, frame_count - 1), n).astype(int).tolist() for idx in indices: cap.set(cv2.CAP_PROP_POS_FRAMES, idx) ok, frame = cap.read() if ok and frame is not None: frames.append(frame) cap.release() return frames, metadef _bbox_area_ratio(bbox: list[int], width: int, height: int) -> float: x, y, w, h = bbox return max(0, w) * max(0, h) / max(1, width * height)def _clip_bbox(bbox: list[int], width: int, height: int) -> list[int]: x, y, w, h = [int(v) for v in bbox] x = max(0, min(x, width - 1)) y = max(0, min(y, height - 1)) w = max(1, min(w, width - x)) h = max(1, min(h, height - y)) return [x, y, w, h]def _iou(a: list[int], b: list[int]) -> float: ax, ay, aw, ah = a bx, by, bw, bh = b ax2, ay2 = ax + aw, ay + ah bx2, by2 = bx + bw, by + bh ix1, iy1 = max(ax, bx), max(ay, by) ix2, iy2 = min(ax2, bx2), min(ay2, by2) iw, ih = max(0, ix2 - ix1), max(0, iy2 - iy1) inter = iw * ih union = aw * ah + bw * bh - inter return inter / union if union > 0 else 0.0def _merge_bboxes(boxes: list[list[int]]) -> list[int]: if not boxes: return [0, 0, 1, 1] xs = [b[0] for b in boxes] ys = [b[1] for b in boxes] x2s = [b[0] + b[2] for b in boxes] y2s = [b[1] + b[3] for b in boxes] x1 = int(np.median(xs)) y1 = int(np.median(ys)) x2 = int(np.median(x2s)) y2 = int(np.median(y2s)) return [x1, y1, max(1, x2 - x1), max(1, y2 - y1)]def _detect_black_bars_single(frame: np.ndarray) -> list[dict[str, Any]]: h, w = frame.shape[:2] gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY) max_top = int(h * 0.25) max_bottom = int(h * 0.25) max_left = int(w * 0.20) max_right = int(w * 0.20) def is_black_band(region: np.ndarray) -> bool: if region.size == 0: return False mean = float(np.mean(region)) std = float(np.std(region)) dark_ratio = float(np.mean(region < 28)) return mean < 35 and std < 18 and dark_ratio > 0.72 boxes: list[dict[str, Any]] = [] step_y = max(4, h // 200) top = 0 for yy in range(0, max_top, step_y): if is_black_band(gray[yy : yy + step_y, :]): top = yy + step_y else: break if top >= max(12, int(h * 0.015)): boxes.append({\"type\": \"black_bar_top\", \"bbox\": [0, 0, w, top]}) bottom = 0 for yy in range(h, h - max_bottom, -step_y): y1 = max(0, yy - step_y) if is_black_band(gray[y1:yy, :]): bottom = h - y1 else: break if bottom >= max(12, int(h * 0.015)): boxes.append({\"type\": \"black_bar_bottom\", \"bbox\": [0, h - bottom, w, bottom]}) step_x = max(4, w // 200) left = 0 for xx in range(0, max_left, step_x): if is_black_band(gray[:, xx : xx + step_x]): left = xx + step_x else: break if left >= max(12, int(w * 0.015)): boxes.append({\"type\": \"black_bar_left\", \"bbox\": [0, 0, left, h]}) right = 0 for xx in range(w, w - max_right, -step_x): x1 = max(0, xx - step_x) if is_black_band(gray[:, x1:xx]): right = w - x1 else: break if right >= max(12, int(w * 0.015)): boxes.append({\"type\": \"black_bar_right\", \"bbox\": [w - right, 0, right, h]}) return boxesdef _detect_subtitle_candidates_single(frame: np.ndarray) -> list[dict[str, Any]]: h, w = frame.shape[:2] y0 = int(h * 0.52) roi = frame[y0:h, :] gray = cv2.cvtColor(roi, cv2.COLOR_BGR2GRAY) gray = cv2.GaussianBlur(gray, (3, 3), 0) grad_x = cv2.Sobel(gray, cv2.CV_16S, 1, 0, ksize=3) grad_y = cv2.Sobel(gray, cv2.CV_16S, 0, 1, ksize=3) abs_grad = cv2.convertScaleAbs(cv2.addWeighted(cv2.convertScaleAbs(grad_x), 0.7, cv2.convertScaleAbs(grad_y), 0.3, 0)) _, th = cv2.threshold(abs_grad, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU) kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (25, 5)) closed = cv2.morphologyEx(th, cv2.MORPH_CLOSE, kernel, iterations=2) contours, _ = cv2.findContours(closed, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE) boxes: list[dict[str, Any]] = [] for c in contours: x, y, bw, bh = cv2.boundingRect(c) area = bw * bh full_y = y + y0 if bw < int(w * 0.18): continue if bh < 18 or bh > int(h * 0.20): continue if area < int(w * h * 0.002): continue if full_y < int(h * 0.55): continue aspect = bw / max(1, bh) if aspect < 3.0: continue pad_x = int(bw * 0.04) pad_y = int(bh * 0.35) bbox = _clip_bbox([x - pad_x, full_y - pad_y, bw + 2 * pad_x, bh + 2 * pad_y], w, h) boxes.append({\"type\": \"subtitle_candidate\", \"bbox\": bbox}) if not boxes: return [] merged = _merge_bboxes([b[\"bbox\"] for b in boxes]) merged = _clip_bbox(merged, w, h) return [{\"type\": \"subtitle_candidate\", \"bbox\": merged}]def _detect_corner_logo_candidates_single(frame: np.ndarray) -> list[dict[str, Any]]: h, w = frame.shape[:2] cw = int(w * 0.28) ch = int(h * 0.18) zones = [ (\"corner_logo_top_left\", 0, 0, cw, ch), (\"corner_logo_top_right\", w - cw, 0, cw, ch), (\"corner_logo_bottom_left\", 0, h - ch, cw, ch), (\"corner_logo_bottom_right\", w - cw, h - ch, cw, ch), ] boxes: list[dict[str, Any]] = [] for typ, zx, zy, zw, zh in zones: roi = frame[zy : zy + zh, zx : zx + zw] if roi.size == 0: continue gray = cv2.cvtColor(roi, cv2.COLOR_BGR2GRAY) gray = cv2.GaussianBlur(gray, (3, 3), 0) edges = cv2.Canny(gray, 70, 170) edge_density = float(np.mean(edges > 0)) if edge_density < 0.018: continue kernel = cv2.getStructuringElement(cv2.MORPH_RECT, (7, 7)) closed = cv2.morphologyEx(edges, cv2.MORPH_CLOSE, kernel, iterations=1) contours, _ = cv2.findContours(closed, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE) local_boxes: list[list[int]] = [] for c in contours: x, y, bw, bh = cv2.boundingRect(c) area = bw * bh if area < zw * zh * 0.005: continue if area > zw * zh * 0.55: continue if bw < 18 or bh < 12: continue local_boxes.append([zx + x, zy + y, bw, bh]) if not local_boxes: continue merged = _merge_bboxes(local_boxes) px = int(merged[2] * 0.30) py = int(merged[3] * 0.45) bbox = _clip_bbox([merged[0] - px, merged[1] - py, merged[2] + 2 * px, merged[3] + 2 * py], w, h) boxes.append({\"type\": typ, \"bbox\": bbox}) return boxesdef _cluster_candidates(candidates: list[dict[str, Any]], sampled_frames: int, width: int, height: int) -> list[dict[str, Any]]: clusters: list[dict[str, Any]] = [] for cand in candidates: typ = cand[\"type\"] bbox = cand[\"bbox\"] matched = None for cl in clusters: if cl[\"type\"] != typ: continue if _iou(cl[\"bbox\"], bbox) >= 0.25: matched = cl break if matched is None: clusters.append({\"type\": typ, \"boxes\": [bbox], \"bbox\": bbox, \"hits\": 1}) else: matched[\"boxes\"].append(bbox) matched[\"hits\"] += 1 matched[\"bbox\"] = _merge_bboxes(matched[\"boxes\"]) out: list[dict[str, Any]] = [] min_hits = 1 if sampled_frames <= 1 else max(2, math.ceil(sampled_frames * 0.25)) for cl in clusters: if cl[\"hits\"] < min_hits: continue bbox = _clip_bbox(cl[\"bbox\"], width, height) area_ratio = _bbox_area_ratio(bbox, width, height) presence = min(1.0, cl[\"hits\"] / max(1, sampled_frames)) typ = cl[\"type\"] if typ.startswith(\"black_bar\"): action = \"crop\" base_conf = 0.80 elif typ == \"subtitle_candidate\": action = \"blur_patch\" base_conf = 0.66 else: action = \"blur_patch\" if area_ratio > 0.01 else \"inpaint_or_blur\" base_conf = 0.58 confidence = round(min(0.98, base_conf + 0.28 * presence), 3) review = False reason = \"ok\" if area_ratio > 0.15: review = True action = \"review\" reason = \"mask_area_gt_15_percent\" out.append( { \"type\": typ, \"bbox\": bbox, \"area_ratio\": round(area_ratio, 6), \"frame_hits\": int(cl[\"hits\"]), \"sampled_frames\": sampled_frames, \"confidence\": confidence, \"recommended_action\": action, \"review_required\": review, \"reason\": reason, } ) return outdef detect_asset(asset: dict[str, Any], out_dir: Path, max_frames: int = 16) -> dict[str, Any]: start = _now_ms() path = Path(asset[\"path\"]) asset_id = str(asset[\"asset_id\"]) frames, meta = _sample_frames(path, max_frames=max_frames) report: dict[str, Any] = { \"asset_id\": asset_id, \"path\": str(path), \"status\": \"PASS\", \"media\": meta, \"sampled_frames\": len(frames), \"masks\": [], \"errors\": [], \"runtime_ms\": 0, } if not frames: report[\"status\"] = \"FAILED\" report[\"errors\"].append(meta.get(\"error\", \"no_frames\")) report[\"runtime_ms\"] = _now_ms() - start return report resized_frames: list[np.ndarray] = [] for f in frames: rf, _, _ = _resize_for_detection(f) resized_frames.append(rf) h, w = resized_frames[0].shape[:2] candidates: list[dict[str, Any]] = [] try: for frame in resized_frames: candidates.extend(_detect_black_bars_single(frame)) candidates.extend(_detect_subtitle_candidates_single(frame)) candidates.extend(_detect_corner_logo_candidates_single(frame)) except Exception as e: report[\"status\"] = \"DEGRADED_PASS\" report[\"errors\"].append(f\"detection_error:{repr(e)}\") clusters = _cluster_candidates(candidates, len(resized_frames), w, h) masks: list[dict[str, Any]] = [] for idx, c in enumerate(clusters, start=1): m = CleanMask( mask_id=f\"{asset_id}_mask_{idx:03d}\", asset_id=asset_id, type=c[\"type\"], bbox=c[\"bbox\"], confidence=float(c[\"confidence\"]), recommended_action=c[\"recommended_action\"], area_ratio=float(c[\"area_ratio\"]), frame_hits=int(c[\"frame_hits\"]), sampled_frames=int(c[\"sampled_frames\"]), review_required=bool(c[\"review_required\"]), reason=str(c[\"reason\"]), ) masks.append(asdict(m)) report[\"masks\"] = masks report[\"runtime_ms\"] = _now_ms() - start _json_dump(out_dir / f\"{asset_id}_clean_detection_report.json\", report) return reportclass CleanDetectionEngine: name = \"clean_detection\" def __init__(self, output_dir: str | Path | None = None, max_frames: int = 16): self.output_dir = Path(output_dir) if output_dir else Path(\"reports\") / \"clean_detection\" self.max_frames = max_frames def run(self, ctx: dict[str, Any]) -> dict[str, Any]: _safe_mkdir(self.output_dir) assets = _get_assets_from_ctx(ctx) all_reports: list[dict[str, Any]] = [] all_masks: list[dict[str, Any]] = [] start = _now_ms() for asset in assets: try: r = detect_asset(asset, self.output_dir, max_frames=self.max_frames) all_reports.append(r) all_masks.extend(r.get(\"masks\", [])) except Exception as e: all_reports.append( { \"asset_id\": asset.get(\"asset_id\", \"unknown\"), \"path\": asset.get(\"path\", \"\"), \"status\": \"FAILED\", \"errors\": [repr(e)], \"masks\": [], } ) summary = { \"engine\": self.name, \"status\": \"PASS\", \"asset_count\": len(assets), \"mask_count\": len(all_masks), \"runtime_ms\": _now_ms() - start, \"reports\": all_reports, } _json_dump(self.output_dir / \"clean_detection_summary.json\", summary) ctx[\"clean_detection_reports\"] = all_reports ctx[\"clean_masks\"] = all_masks ctx[\"masks\"] = all_masks return ctxdef run(ctx: dict[str, Any]) -> dict[str, Any]: return CleanDetectionEngine().run(ctx)def main() -> int: parser = argparse.ArgumentParser() parser.add_argument(\"--input\", required=True, help=\"输入文件或目录\") parser.add_argument(\"--output\", default=\"reports/clean_detection\") parser.add_argument(\"--max-frames\", type=int, default=16) args = parser.parse_args() inp = Path(args.input) if inp.is_dir(): assets = [] for p in inp.rglob(\"*\"): if p.suffix.lower() in IMAGE_EXTS | VIDEO_EXTS: assets.append({\"asset_id\": _asset_id_from_path(p), \"path\": str(p)}) else: assets = [{\"asset_id\": _asset_id_from_path(inp), \"path\": str(inp)}] ctx = {\"assets\": assets} CleanDetectionEngine(args.output, max_frames=args.max_frames).run(ctx) print(json.dumps({\"mask_count\": len(ctx.get(\"clean_masks\", [])), \"output\": args.output}, ensure_ascii=False, indent=2)) return 0if __name__ == \"__main__\": raise SystemExit(main())\n\napp/material_engine/engines/clean_rebuild.py\nPythonfrom __future__ import annotationsimport argparseimport jsonimport osimport shutilimport subprocessimport timeimport hashlibfrom dataclasses import dataclass, asdictfrom pathlib import Pathfrom typing import Anyimport cv2import numpy as npIMAGE_EXTS = {\".jpg\", \".jpeg\", \".png\", \".webp\", \".bmp\"}VIDEO_EXTS = {\".mp4\", \".mov\", \".mkv\", \".avi\", \".webm\", \".m4v\"}TARGET_W = 1080TARGET_H = 1920MAX_INPAINT_AREA_RATIO = 0.15MAX_DIRECT_INPAINT_AREA_RATIO = 0.035DEFAULT_FPS = 30.0@dataclassclass CleanAction: mask_id: str asset_id: str type: str bbox: list[int] requested_action: str applied_action: str status: str review_required: bool reason: str area_ratio: float runtime_ms: intdef _now_ms() -> int: return int(time.time() * 1000)def _safe_mkdir(path: Path) -> None: path.mkdir(parents=True, exist_ok=True)def _json_dump(path: Path, data: dict[str, Any]) -> None: _safe_mkdir(path.parent) path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding=\"utf-8\")def _asset_id_from_path(path: Path) -> str: h = hashlib.sha1(str(path).encode(\"utf-8\", errors=\"ignore\")).hexdigest()[:10] return f\"asset_{h}\"def _norm_asset(asset: Any) -> dict[str, Any]: if isinstance(asset, (str, Path)): p = Path(asset) return {\"asset_id\": _asset_id_from_path(p), \"path\": str(p)} if isinstance(asset, dict): p = asset.get(\"path\") or asset.get(\"source_path\") or asset.get(\"file_path\") or asset.get(\"input\") or asset.get(\"output_path\") if not p: raise ValueError(f\"asset 缺少 path/source_path/file_path/input/output_path: {asset}\") aid = asset.get(\"asset_id\") or asset.get(\"id\") or _asset_id_from_path(Path(p)) out = dict(asset) out[\"asset_id\"] = aid out[\"path\"] = str(p) return out raise TypeError(f\"不支持的 asset 类型: {type(asset)}\")def _get_assets_from_ctx(ctx: dict[str, Any]) -> list[dict[str, Any]]: raw = ( ctx.get(\"assets\") or ctx.get(\"asset_pool\") or ctx.get(\"input_assets\") or ctx.get(\"clean_detection_assets\") or [] ) return [_norm_asset(a) for a in raw]def _get_masks_for_asset(ctx: dict[str, Any], asset_id: str) -> list[dict[str, Any]]: masks = ctx.get(\"clean_masks\") or ctx.get(\"masks\") or [] return [m for m in masks if str(m.get(\"asset_id\", \"\")) == str(asset_id)]def _read_image(path: Path) -> np.ndarray | None: data = np.fromfile(str(path), dtype=np.uint8) if data.size == 0: return None return cv2.imdecode(data, cv2.IMREAD_COLOR)def _write_image(path: Path, img: np.ndarray) -> bool: _safe_mkdir(path.parent) ext = path.suffix.lower() if ext not in IMAGE_EXTS: ext = \".png\" ok, buf = cv2.imencode(ext, img) if not ok: return False buf.tofile(str(path)) return Truedef _clip_bbox(bbox: list[int], width: int, height: int) -> list[int]: x, y, w, h = [int(v) for v in bbox] x = max(0, min(x, width - 1)) y = max(0, min(y, height - 1)) w = max(1, min(w, width - x)) h = max(1, min(h, height - y)) return [x, y, w, h]def _bbox_area_ratio(bbox: list[int], width: int, height: int) -> float: x, y, w, h = bbox return max(0, w) * max(0, h) / max(1, width * height)def _expand_bbox(bbox: list[int], width: int, height: int, px: int) -> list[int]: x, y, w, h = bbox return _clip_bbox([x - px, y - px, w + 2 * px, h + 2 * px], width, height)def _is_black_bar(mask_type: str) -> bool: return mask_type.startswith(\"black_bar\")def _black_bar_crop_rect(masks: list[dict[str, Any]], width: int, height: int) -> tuple[int, int, int, int]: left = 0 top = 0 right = width bottom = height for m in masks: typ = str(m.get(\"type\", \"\")) if not _is_black_bar(typ): continue bbox = _clip_bbox(list(map(int, m.get(\"bbox\", [0, 0, 1, 1]))), width, height) x, y, w, h = bbox if typ == \"black_bar_top\": top = max(top, y + h) elif typ == \"black_bar_bottom\": bottom = min(bottom, y) elif typ == \"black_bar_left\": left = max(left, x + w) elif typ == \"black_bar_right\": right = min(right, x) if right - left < width * 0.45 or bottom - top < height * 0.45: return 0, 0, width, height return left, top, right, bottomdef _crop_black_bars(frame: np.ndarray, masks: list[dict[str, Any]]) -> tuple[np.ndarray, list[str]]: h, w = frame.shape[:2] left, top, right, bottom = _black_bar_crop_rect(masks, w, h) if left == 0 and top == 0 and right == w and bottom == h: return frame, [] cropped = frame[top:bottom, left:right] if cropped.size == 0: return frame, [\"crop_failed_empty\"] return cropped, [f\"crop_rect:{left},{top},{right},{bottom}\"]def _apply_blur_patch(frame: np.ndarray, bbox: list[int]) -> np.ndarray: h, w = frame.shape[:2] x, y, bw, bh = _expand_bbox(bbox, w, h, px=max(4, int(min(w, h) * 0.004))) roi = frame[y : y + bh, x : x + bw] if roi.size == 0: return frame k = max(21, int(min(bw, bh) * 0.35)) if k % 2 == 0: k += 1 k = min(k, 101) if k % 2 == 0: k -= 1 k = max(21, k) blurred = cv2.GaussianBlur(roi, (k, k), 0) mask = np.zeros((bh, bw), dtype=np.float32) cv2.rectangle(mask, (0, 0), (bw, bh), 1.0, -1) feather = max(5, int(min(bw, bh) * 0.08)) if feather % 2 == 0: feather += 1 mask = cv2.GaussianBlur(mask, (feather, feather), 0) mask = np.clip(mask, 0.0, 1.0)[..., None] roi_f = roi.astype(np.float32) blur_f = blurred.astype(np.float32) out = roi_f * (1.0 - mask) + blur_f * mask frame[y : y + bh, x : x + bw] = np.clip(out, 0, 255).astype(np.uint8) return framedef _apply_inpaint(frame: np.ndarray, bbox: list[int]) -> np.ndarray: h, w = frame.shape[:2] x, y, bw, bh = _expand_bbox(bbox, w, h, px=max(3, int(min(w, h) * 0.003))) mask = np.zeros((h, w), dtype=np.uint8) cv2.rectangle(mask, (x, y), (x + bw, y + bh), 255, -1) return cv2.inpaint(frame, mask, 3, cv2.INPAINT_TELEA)def _choose_action(mask: dict[str, Any], frame_w: int, frame_h: int) -> tuple[str, bool, str]: typ = str(mask.get(\"type\", \"unknown\")) bbox = _clip_bbox(list(map(int, mask.get(\"bbox\", [0, 0, 1, 1]))), frame_w, frame_h) area_ratio = _bbox_area_ratio(bbox, frame_w, frame_h) if bool(mask.get(\"review_required\", False)): return \"review\", True, str(mask.get(\"reason\", \"mask_marked_review\")) if area_ratio > MAX_INPAINT_AREA_RATIO: return \"review\", True, \"mask_area_gt_15_percent\" requested = str(mask.get(\"recommended_action\", \"\")) if _is_black_bar(typ): return \"crop\", False, \"black_bar_crop\" if requested == \"review\": return \"review\", True, str(mask.get(\"reason\", \"recommended_review\")) if area_ratio <= MAX_DIRECT_INPAINT_AREA_RATIO and (\"logo\" in typ or \"corner\" in typ or \"watermark\" in typ or requested == \"inpaint_or_blur\"): return \"inpaint\", False, \"small_mask_inpaint\" if area_ratio <= MAX_INPAINT_AREA_RATIO: return \"blur_patch\", False, \"safe_blur_patch\" return \"review\", True, \"unsupported_mask\"def _normalize_frame_to_1080x1920(frame: np.ndarray) -> np.ndarray: h, w = frame.shape[:2] if w == TARGET_W and h == TARGET_H: return frame scale = min(TARGET_W / w, TARGET_H / h) nw = max(1, int(w * scale)) nh = max(1, int(h * scale)) resized = cv2.resize(frame, (nw, nh), interpolation=cv2.INTER_AREA if scale < 1 else cv2.INTER_CUBIC) canvas = np.zeros((TARGET_H, TARGET_W, 3), dtype=np.uint8) x = (TARGET_W - nw) // 2 y = (TARGET_H - nh) // 2 canvas[y : y + nh, x : x + nw] = resized if x > 0 or y > 0: bg = cv2.resize(frame, (TARGET_W, TARGET_H), interpolation=cv2.INTER_AREA) bg = cv2.GaussianBlur(bg, (81, 81), 0) bg[y : y + nh, x : x + nw] = resized return bg return canvasdef _process_frame(frame: np.ndarray, masks: list[dict[str, Any]]) -> tuple[np.ndarray, list[CleanAction]]: start = _now_ms() original_h, original_w = frame.shape[:2] actions: list[CleanAction] = [] black_masks = [m for m in masks if _is_black_bar(str(m.get(\"type\", \"\")))] non_black_masks = [m for m in masks if not _is_black_bar(str(m.get(\"type\", \"\")))] frame2 = frame.copy() if black_masks: try: before_shape = frame2.shape[:2] frame2, notes = _crop_black_bars(frame2, black_masks) status = \"PASS\" if notes else \"SKIPPED\" reason = \";\".join(notes) if notes else \"no_crop_needed\" except Exception as e: status = \"FALLBACK\" reason = f\"crop_failed:{repr(e)}\" frame2 = frame.copy() for m in black_masks: bbox = _clip_bbox(list(map(int, m.get(\"bbox\", [0, 0, 1, 1]))), original_w, original_h) actions.append( CleanAction( mask_id=str(m.get(\"mask_id\", \"\")), asset_id=str(m.get(\"asset_id\", \"\")), type=str(m.get(\"type\", \"\")), bbox=bbox, requested_action=str(m.get(\"recommended_action\", \"crop\")), applied_action=\"crop\", status=status, review_required=False if status == \"PASS\" else True, reason=reason, area_ratio=_bbox_area_ratio(bbox, original_w, original_h), runtime_ms=_now_ms() - start, ) ) h, w = frame2.shape[:2] for m in non_black_masks: action_start = _now_ms() bbox = _clip_bbox(list(map(int, m.get(\"bbox\", [0, 0, 1, 1]))), w, h) area_ratio = _bbox_area_ratio(bbox, w, h) requested = str(m.get(\"recommended_action\", \"\")) applied, review, reason = _choose_action(m, w, h) status = \"PASS\" if review: status = \"REVIEW\" else: try: if applied == \"inpaint\": frame2 = _apply_inpaint(frame2, bbox) elif applied == \"blur_patch\": frame2 = _apply_blur_patch(frame2, bbox) else: status = \"REVIEW\" review = True reason = f\"unsupported_action:{applied}\" except Exception as e: try: frame2 = _apply_blur_patch(frame2, bbox) applied = \"blur_patch\" status = \"FALLBACK\" reason = f\"inpaint_or_action_failed_blur_fallback:{repr(e)}\" review = False except Exception as e2: applied = \"review\" status = \"REVIEW\" reason = f\"all_actions_failed:{repr(e)};{repr(e2)}\" review = True actions.append( CleanAction( mask_id=str(m.get(\"mask_id\", \"\")), asset_id=str(m.get(\"asset_id\", \"\")), type=str(m.get(\"type\", \"\")), bbox=bbox, requested_action=requested, applied_action=applied, status=status, review_required=review, reason=reason, area_ratio=area_ratio, runtime_ms=_now_ms() - action_start, ) ) frame2 = _normalize_frame_to_1080x1920(frame2) return frame2, actionsdef _find_ffmpeg(root: Path | None = None) -> Path | None: candidates: list[Path] = [] if root: candidates.extend( [ root / \"_bundled\" / \"ffmpeg\" / \"bin\" / \"ffmpeg.exe\", root / \"_bundled\" / \"ffmpeg\" / \"ffmpeg.exe\", ] ) env = os.environ.get(\"PHS_FFMPEG_BIN\") if env: candidates.append(Path(env) / \"ffmpeg.exe\") which = shutil.which(\"ffmpeg\") if which: candidates.append(Path(which)) for p in candidates: if p.exists(): return p return Nonedef _mux_audio(ffmpeg: Path | None, original_video: Path, silent_video: Path, output_video: Path) -> bool: if not ffmpeg or not ffmpeg.exists(): shutil.copy2(silent_video, output_video) return False cmd = [ str(ffmpeg), \"-y\", \"-i\", str(silent_video), \"-i\", str(original_video), \"-map\", \"0:v:0\", \"-map\", \"1:a?\", \"-c:v\", \"copy\", \"-c:a\", \"aac\", \"-shortest\", str(output_video), ] try: p = subprocess.run(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True, encoding=\"utf-8\", errors=\"replace\", timeout=120) if p.returncode == 0 and output_video.exists(): return True except Exception: pass shutil.copy2(silent_video, output_video) return Falsedef _process_image(asset: dict[str, Any], masks: list[dict[str, Any]], output_dir: Path) -> dict[str, Any]: start = _now_ms() path = Path(asset[\"path\"]) aid = str(asset[\"asset_id\"]) out_path = output_dir / f\"{aid}_clean.png\" report: dict[str, Any] = { \"asset_id\": aid, \"input\": str(path), \"output\": str(out_path), \"media_type\": \"image\", \"status\": \"PASS\", \"actions\": [], \"fallback_used\": False, \"review_required\": False, \"errors\": [], \"runtime_ms\": 0, } img = _read_image(path) if img is None: report[\"status\"] = \"FAILED\" report[\"errors\"].append(\"read_image_failed\") report[\"runtime_ms\"] = _now_ms() - start return report try: clean, actions = _process_frame(img, masks) ok = _write_image(out_path, clean) if not ok: raise RuntimeError(\"write_image_failed\") report[\"actions\"] = [asdict(a) for a in actions] report[\"review_required\"] = any(a.review_required for a in actions) except Exception as e: fallback_path = output_dir / f\"{aid}_fallback_original{path.suffix.lower() or '.png'}\" shutil.copy2(path, fallback_path) report[\"status\"] = \"FALLBACK\" report[\"output\"] = str(fallback_path) report[\"fallback_used\"] = True report[\"review_required\"] = True report[\"errors\"].append(repr(e)) report[\"runtime_ms\"] = _now_ms() - start return reportdef _process_video(asset: dict[str, Any], masks: list[dict[str, Any]], output_dir: Path, root: Path | None = None) -> dict[str, Any]: start = _now_ms() path = Path(asset[\"path\"]) aid = str(asset[\"asset_id\"]) temp_no_audio = output_dir / f\"{aid}_clean_no_audio.mp4\" out_path = output_dir / f\"{aid}_clean.mp4\" report: dict[str, Any] = { \"asset_id\": aid, \"input\": str(path), \"output\": str(out_path), \"media_type\": \"video\", \"status\": \"PASS\", \"actions\": [], \"fallback_used\": False, \"audio_muxed\": False, \"review_required\": False, \"errors\": [], \"runtime_ms\": 0, } cap = cv2.VideoCapture(str(path)) if not cap.isOpened(): fallback_path = output_dir / f\"{aid}_fallback_original{path.suffix.lower()}\" shutil.copy2(path, fallback_path) report[\"status\"] = \"FALLBACK\" report[\"output\"] = str(fallback_path) report[\"fallback_used\"] = True report[\"review_required\"] = True report[\"errors\"].append(\"video_open_failed\") report[\"runtime_ms\"] = _now_ms() - start return report fps = float(cap.get(cv2.CAP_PROP_FPS) or DEFAULT_FPS) if fps <= 1 or fps > 120: fps = DEFAULT_FPS fourcc = cv2.VideoWriter_fourcc(*\"mp4v\") _safe_mkdir(output_dir) writer = cv2.VideoWriter(str(temp_no_audio), fourcc, fps, (TARGET_W, TARGET_H)) if not writer.isOpened(): cap.release() fallback_path = output_dir / f\"{aid}_fallback_original{path.suffix.lower()}\" shutil.copy2(path, fallback_path) report[\"status\"] = \"FALLBACK\" report[\"output\"] = str(fallback_path) report[\"fallback_used\"] = True report[\"review_required\"] = True report[\"errors\"].append(\"video_writer_open_failed\") report[\"runtime_ms\"] = _now_ms() - start return report all_actions_by_key: dict[str, CleanAction] = {} frame_count = 0 failed_frames = 0 try: while True: ok, frame = cap.read() if not ok or frame is None: break try: clean, actions = _process_frame(frame, masks) writer.write(clean) for a in actions: key = a.mask_id or f\"{a.type}:{a.bbox}\" old = all_actions_by_key.get(key) if old is None: all_actions_by_key[key] = a else: if a.status == \"REVIEW\": old.status = \"REVIEW\" old.review_required = True elif a.status == \"FALLBACK\" and old.status == \"PASS\": old.status = \"FALLBACK\" old.runtime_ms += a.runtime_ms except Exception as e: failed_frames += 1 fallback_frame = _normalize_frame_to_1080x1920(frame) writer.write(fallback_frame) report[\"errors\"].append(f\"frame_{frame_count}_fallback:{repr(e)}\") frame_count += 1 except Exception as e: report[\"status\"] = \"DEGRADED_PASS\" report[\"errors\"].append(f\"video_loop_error:{repr(e)}\") finally: cap.release() writer.release() actions_list = list(all_actions_by_key.values()) report[\"actions\"] = [asdict(a) for a in actions_list] report[\"review_required\"] = any(a.review_required for a in actions_list) report[\"frame_count\"] = frame_count report[\"failed_frames\"] = failed_frames ffmpeg = _find_ffmpeg(root) muxed = _mux_audio(ffmpeg, path, temp_no_audio, out_path) report[\"audio_muxed\"] = muxed if not out_path.exists(): if temp_no_audio.exists(): shutil.copy2(temp_no_audio, out_path) else: fallback_path = output_dir / f\"{aid}_fallback_original{path.suffix.lower()}\" shutil.copy2(path, fallback_path) report[\"status\"] = \"FALLBACK\" report[\"output\"] = str(fallback_path) report[\"fallback_used\"] = True report[\"review_required\"] = True report[\"errors\"].append(\"output_missing_after_processing\") if failed_frames > 0 and report[\"status\"] == \"PASS\": report[\"status\"] = \"DEGRADED_PASS\" report[\"runtime_ms\"] = _now_ms() - start return reportclass CleanRebuildEngine: name = \"clean_rebuild\" def __init__(self, output_dir: str | Path | None = None, root: str | Path | None = None): self.output_dir = Path(output_dir) if output_dir else Path(\"runs\") / \"clean_rebuild\" self.root = Path(root) if root else Path.cwd() def run(self, ctx: dict[str, Any]) -> dict[str, Any]: _safe_mkdir(self.output_dir) assets = _get_assets_from_ctx(ctx) reports: list[dict[str, Any]] = [] clean_assets: list[dict[str, Any]] = [] start = _now_ms() for asset in assets: path = Path(asset[\"path\"]) aid = str(asset[\"asset_id\"]) masks = _get_masks_for_asset(ctx, aid) try: if not masks: out_path = self.output_dir / f\"{aid}_passthrough{path.suffix.lower()}\" shutil.copy2(path, out_path) report = { \"asset_id\": aid, \"input\": str(path), \"output\": str(out_path), \"media_type\": \"unknown\", \"status\": \"PASS\", \"actions\": [], \"fallback_used\": True, \"review_required\": False, \"errors\": [], \"reason\": \"no_masks_passthrough\", \"runtime_ms\": 0, } elif path.suffix.lower() in IMAGE_EXTS: report = _process_image(asset, masks, self.output_dir) elif path.suffix.lower() in VIDEO_EXTS: report = _process_video(asset, masks, self.output_dir, root=self.root) else: out_path = self.output_dir / f\"{aid}_unsupported_passthrough{path.suffix.lower()}\" shutil.copy2(path, out_path) report = { \"asset_id\": aid, \"input\": str(path), \"output\": str(out_path), \"media_type\": \"unsupported\", \"status\": \"FALLBACK\", \"actions\": [], \"fallback_used\": True, \"review_required\": True, \"errors\": [f\"unsupported_ext:{path.suffix.lower()}\"], \"runtime_ms\": 0, } except Exception as e: fallback_path = self.output_dir / f\"{aid}_fatal_fallback{path.suffix.lower()}\" try: shutil.copy2(path, fallback_path) except Exception: pass report = { \"asset_id\": aid, \"input\": str(path), \"output\": str(fallback_path), \"media_type\": \"unknown\", \"status\": \"FALLBACK\", \"actions\": [], \"fallback_used\": True, \"review_required\": True, \"errors\": [repr(e)], \"runtime_ms\": 0, } reports.append(report) if report.get(\"output\"): clean_assets.append( { \"asset_id\": aid, \"source_asset_id\": aid, \"path\": report[\"output\"], \"output_path\": report[\"output\"], \"status\": report.get(\"status\", \"UNKNOWN\"), \"review_required\": report.get(\"review_required\", False), } ) _json_dump(self.output_dir / f\"{aid}_clean_rebuild_report.json\", report) summary = { \"engine\": self.name, \"status\": \"PASS\", \"asset_count\": len(assets), \"clean_asset_count\": len(clean_assets), \"runtime_ms\": _now_ms() - start, \"reports\": reports, } _json_dump(self.output_dir / \"clean_rebuild_summary.json\", summary) ctx[\"clean_rebuild_reports\"] = reports ctx[\"clean_assets\"] = clean_assets ctx[\"clean_rebuild_summary\"] = summary return ctxdef run(ctx: dict[str, Any]) -> dict[str, Any]: return CleanRebuildEngine().run(ctx)def _load_masks(masks_json: Path) -> list[dict[str, Any]]: if not masks_json.exists(): return [] data = json.loads(masks_json.read_text(encoding=\"utf-8\")) if isinstance(data, dict): if \"masks\" in data and isinstance(data[\"masks\"], list): return data[\"masks\"] if \"reports\" in data: masks: list[dict[str, Any]] = [] for r in data.get(\"reports\", []): masks.extend(r.get(\"masks\", [])) return masks if isinstance(data, list): return data return []def main() -> int: parser = argparse.ArgumentParser() parser.add_argument(\"--input\", required=True, help=\"输入文件或目录\") parser.add_argument(\"--masks\", required=True, help=\"clean_detection 输出的 JSON，可为 summary 或单 asset report\") parser.add_argument(\"--output\", default=\"runs/clean_rebuild\") parser.add_argument(\"--root\", default=\".\") args = parser.parse_args() inp = Path(args.input) if inp.is_dir(): assets = [] for p in inp.rglob(\"*\"): if p.suffix.lower() in IMAGE_EXTS | VIDEO_EXTS: assets.append({\"asset_id\": _asset_id_from_path(p), \"path\": str(p)}) else: assets = [{\"asset_id\": _asset_id_from_path(inp), \"path\": str(inp)}] masks = _load_masks(Path(args.masks)) ctx = { \"assets\": assets, \"clean_masks\": masks, } CleanRebuildEngine(output_dir=args.output, root=args.root).run(ctx) print( json.dumps( { \"clean_asset_count\": len(ctx.get(\"clean_assets\", [])), \"output\": args.output, }, ensure_ascii=False, indent=2, ) ) return 0if __name__ == \"__main__\": raise SystemExit(main())"
}
