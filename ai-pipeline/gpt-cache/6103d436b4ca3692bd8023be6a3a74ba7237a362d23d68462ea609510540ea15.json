{
  "timestamp": "2026-06-28T20:50:33.9689390+08:00",
  "response": "Python# visual_fingerprint_engine.pyfrom __future__ import annotationsimport argparseimport hashlibimport jsonimport mathimport timefrom dataclasses import asdict, dataclass, fieldfrom pathlib import Pathfrom typing import Anyimport cv2import numpy as npIMAGE_EXTS = {\".jpg\", \".jpeg\", \".png\", \".webp\", \".bmp\"}VIDEO_EXTS = {\".mp4\", \".mov\", \".mkv\", \".avi\", \".webm\", \".m4v\"}PHASH_SIZE = 32PHASH_LOW_SIZE = 8DHASH_W = 9DHASH_H = 8def now_ms() -> int: return int(time.time() * 1000)def mkdir(path: Path) -> None: path.mkdir(parents=True, exist_ok=True)def dump_json(path: Path, data: Any) -> None: mkdir(path.parent) path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding=\"utf-8\")def sha1_text(text: str, n: int = 16) -> str: return hashlib.sha1(text.encode(\"utf-8\", errors=\"ignore\")).hexdigest()[:n]def asset_id_for(path: Path) -> str: try: st = path.stat() raw = f\"{path.resolve()}:{st.st_size}:{st.st_mtime_ns}\" except Exception: raw = str(path) return \"asset_\" + sha1_text(raw, 12)def to_hex_bits(bits: np.ndarray) -> str: bits = bits.astype(np.uint8).reshape(-1) val = 0 out = [] for i, b in enumerate(bits): val = (val << 1) | int(b) if (i + 1) % 4 == 0: out.append(format(val, \"x\")) val = 0 if len(bits) % 4: val <<= 4 - (len(bits) % 4) out.append(format(val, \"x\")) return \"\".join(out)def read_image(path: Path) -> np.ndarray | None: data = np.fromfile(str(path), dtype=np.uint8) if data.size == 0: return None return cv2.imdecode(data, cv2.IMREAD_COLOR)def phash(img: np.ndarray) -> str: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) small = cv2.resize(gray, (PHASH_SIZE, PHASH_SIZE), interpolation=cv2.INTER_AREA).astype(np.float32) dct = cv2.dct(small) low = dct[:PHASH_LOW_SIZE, :PHASH_LOW_SIZE].flatten() med = float(np.median(low[1:])) if low.size > 1 else 0.0 bits = low > med if bits.size: bits[0] = False return to_hex_bits(bits)def dhash(img: np.ndarray) -> str: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) small = cv2.resize(gray, (DHASH_W, DHASH_H), interpolation=cv2.INTER_AREA) diff = small[:, 1:] > small[:, :-1] return to_hex_bits(diff)def normalize_hist(hist: np.ndarray) -> list[float]: arr = hist.astype(np.float32).reshape(-1) s = float(np.sum(arr)) if s <= 1e-12: return [0.0] * arr.size return (arr / s).round(6).astype(float).tolist()def hsv_hist(img: np.ndarray) -> list[float]: hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV) hist = cv2.calcHist([hsv], [0, 1], None, [16, 8], [0, 180, 0, 256]) return normalize_hist(hist)def edge_hist(img: np.ndarray) -> list[float]: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) edges = cv2.Canny(gray, 80, 180) h, w = edges.shape vals = [] for gy in range(4): for gx in range(4): x1 = gx * w // 4 x2 = (gx + 1) * w // 4 if gx < 3 else w y1 = gy * h // 4 y2 = (gy + 1) * h // 4 if gy < 3 else h roi = edges[y1:y2, x1:x2] vals.append(float(np.mean(roi > 0))) arr = np.asarray(vals, dtype=np.float32) s = float(np.sum(arr)) if s > 1e-12: arr = arr / s return arr.round(6).astype(float).tolist()def brightness(img: np.ndarray) -> float: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) return round(float(np.mean(gray)), 4)def blur_score(img: np.ndarray) -> float: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) return round(float(cv2.Laplacian(gray, cv2.CV_64F).var()), 4)def black_ratio(img: np.ndarray) -> float: gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY) return round(float(np.mean(gray < 16)), 6)def quality_score(bright: float, blur: float, black: float) -> float: score = 100.0 if black > 0.35: score -= 45 elif black > 0.12: score -= 20 if blur < 40: score -= 35 elif blur < 90: score -= 18 if bright < 35 or bright > 230: score -= 25 elif bright < 55 or bright > 210: score -= 10 return round(max(0.0, min(100.0, score)), 3)def crop_rel(img: np.ndarray, box: tuple[float, float, float, float]) -> np.ndarray: h, w = img.shape[:2] x1 = max(0, min(w - 1, int(w * box[0]))) y1 = max(0, min(h - 1, int(h * box[1]))) x2 = max(x1 + 1, min(w, int(w * box[2]))) y2 = max(y1 + 1, min(h, int(h * box[3]))) return img[y1:y2, x1:x2]def tile_hashes(img: np.ndarray) -> dict[str, str]: out: dict[str, str] = {} for gy in range(3): for gx in range(3): out[f\"tile_{gy}_{gx}\"] = phash(crop_rel(img, (gx / 3, gy / 3, (gx + 1) / 3, (gy + 1) / 3))) specials = { \"center\": (0.25, 0.25, 0.75, 0.75), \"top_left\": (0.0, 0.0, 0.35, 0.35), \"top_right\": (0.65, 0.0, 1.0, 0.35), \"bottom_left\": (0.0, 0.65, 0.35, 1.0), \"bottom_right\": (0.65, 0.65, 1.0, 1.0), } for name, box in specials.items(): out[name] = phash(crop_rel(img, box)) return outdef frame_diff(prev: np.ndarray | None, cur: np.ndarray) -> float: if prev is None: return 0.0 a = cv2.cvtColor(prev, cv2.COLOR_BGR2GRAY) b = cv2.cvtColor(cur, cv2.COLOR_BGR2GRAY) if a.shape != b.shape: b = cv2.resize(b, (a.shape[1], a.shape[0]), interpolation=cv2.INTER_AREA) return round(float(np.mean(cv2.absdiff(a, b))) / 255.0, 6)def optical_flow_signature(prev: np.ndarray | None, cur: np.ndarray) -> dict[str, Any]: if prev is None: return {\"flow_mean\": 0.0, \"flow_p95\": 0.0, \"flow_direction_hist\": [0.0] * 8} a = cv2.cvtColor(prev, cv2.COLOR_BGR2GRAY) b = cv2.cvtColor(cur, cv2.COLOR_BGR2GRAY) if a.shape != b.shape: b = cv2.resize(b, (a.shape[1], a.shape[0]), interpolation=cv2.INTER_AREA) target_w = 320 scale = target_w / max(1, a.shape[1]) target_h = max(1, int(a.shape[0] * scale)) a = cv2.resize(a, (target_w, target_h), interpolation=cv2.INTER_AREA) b = cv2.resize(b, (target_w, target_h), interpolation=cv2.INTER_AREA) try: flow = cv2.calcOpticalFlowFarneback(a, b, None, 0.5, 2, 15, 2, 5, 1.2, 0) mag, ang = cv2.cartToPolar(flow[..., 0], flow[..., 1]) hist, _ = np.histogram(ang, bins=8, range=(0, 2 * np.pi), weights=mag) hist = np.asarray(hist, dtype=np.float32) s = float(np.sum(hist)) if s > 1e-12: hist = hist / s return { \"flow_mean\": round(float(np.mean(mag)), 6), \"flow_p95\": round(float(np.percentile(mag, 95)), 6), \"flow_direction_hist\": hist.round(6).astype(float).tolist(), } except Exception: diff = frame_diff(prev, cur) return {\"flow_mean\": diff, \"flow_p95\": diff, \"flow_direction_hist\": [0.0] * 8}def median_hash(hashes: list[str]) -> str: hashes = [h for h in hashes if h] if not hashes: return \"\" bit_len = min(len(h) for h in hashes) * 4 rows = [] for h in hashes: v = int(h[: bit_len // 4], 16) rows.append([(v >> i) & 1 for i in range(bit_len)]) mat = np.asarray(rows, dtype=np.uint8) bits = (np.mean(mat, axis=0) >= 0.5).astype(np.uint8) val = 0 for i, bit in enumerate(bits): val |= int(bit) << i return format(val, f\"0{bit_len // 4}x\")def avg_vectors(vectors: list[list[float]]) -> list[float]: if not vectors: return [] arr = np.asarray(vectors, dtype=np.float32) avg = np.mean(arr, axis=0) s = float(np.sum(avg)) if s > 1e-12: avg = avg / s return avg.round(6).astype(float).tolist()def metadata(path: Path) -> dict[str, Any]: if path.suffix.lower() in IMAGE_EXTS: img = read_image(path) if img is None: return {\"error\": \"read_image_failed\", \"media_type\": \"image\"} h, w = img.shape[:2] return {\"media_type\": \"image\", \"width\": w, \"height\": h, \"fps\": 0.0, \"duration_sec\": 0.0, \"frame_count\": 1} cap = cv2.VideoCapture(str(path)) if not cap.isOpened(): return {\"error\": \"video_open_failed\", \"media_type\": \"video\"} fps = float(cap.get(cv2.CAP_PROP_FPS) or 0.0) frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT) or 0) w = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH) or 0) h = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT) or 0) cap.release() dur = frames / fps if fps > 0 else 0.0 return {\"media_type\": \"video\", \"width\": w, \"height\": h, \"fps\": fps, \"duration_sec\": dur, \"frame_count\": frames}def sample_frames(path: Path, interval_sec: float, max_frames: int) -> tuple[list[tuple[int, float, np.ndarray]], dict[str, Any]]: meta = metadata(path) if meta.get(\"error\"): return [], meta if path.suffix.lower() in IMAGE_EXTS: img = read_image(path) return ([(0, 0.0, img)] if img is not None else []), meta cap = cv2.VideoCapture(str(path)) fps = float(meta.get(\"fps\") or 0.0) frame_count = int(meta.get(\"frame_count\") or 0) duration = float(meta.get(\"duration_sec\") or 0.0) if not cap.isOpened() or fps <= 0 or frame_count <= 0: if cap.isOpened(): ok, frame = cap.read() cap.release() return ([(0, 0.0, frame)] if ok else []), meta return [], {**meta, \"error\": \"no_frames\"} times = np.arange(0.0, max(duration, 0.001), max(0.1, interval_sec)) if len(times) > max_frames: times = np.linspace(0, max(0.0, duration - 0.001), max_frames) out = [] for ts in times: idx = max(0, min(frame_count - 1, int(round(ts * fps)))) cap.set(cv2.CAP_PROP_POS_FRAMES, idx) ok, frame = cap.read() if ok and frame is not None: out.append((idx, idx / fps, frame)) cap.release() return out, meta@dataclassclass VisualFingerprintCard: fingerprint_id: str asset_id: str scene_id: str variant_id: str | None source_path: str media_type: str width: int height: int fps: float duration_sec: float sample_interval_sec: float sampled_frames: int frame_fingerprints: list[dict[str, Any]] scene_phash_median: str scene_dhash_median: str scene_color_hist_avg: list[float] scene_edge_hist_avg: list[float] tile_hashes_median: dict[str, str] motion_signature: dict[str, Any] quality: dict[str, Any] created_at_ms: int = field(default_factory=now_ms) engine: str = \"visual_fingerprint_engine\" engine_version: str = \"1.5.1\"class VisualFingerprintEngine: def __init__(self, output_dir: str | Path = \"reports/visual_fingerprint\", interval_sec: float = 1.0, max_frames: int = 7200): self.output_dir = Path(output_dir) self.interval_sec = interval_sec self.max_frames = max_frames def analyze(self, path: str | Path, asset_id: str | None = None, scene_id: str | None = None, variant_id: str | None = None) -> dict[str, Any]: start = now_ms() p = Path(path) report = {\"status\": \"PASS\", \"path\": str(p), \"card\": None, \"errors\": [], \"runtime_ms\": 0} if not p.exists(): report[\"status\"] = \"FAILED\" report[\"errors\"].append(\"file_missing\") return report if p.suffix.lower() not in IMAGE_EXTS | VIDEO_EXTS: report[\"status\"] = \"FAILED\" report[\"errors\"].append(f\"unsupported_ext:{p.suffix.lower()}\") return report frames, meta = sample_frames(p, self.interval_sec, self.max_frames) if not frames: report[\"status\"] = \"FAILED\" report[\"errors\"].append(meta.get(\"error\", \"no_frames\")) return report aid = asset_id or asset_id_for(p) sid = scene_id or \"scene_\" + sha1_text(str(p.resolve()), 12) fid = \"vf_\" + sha1_text(str(p.resolve()) + str(time.time_ns()), 16) fprints = [] prev = None for idx, ts, frame in frames: br = brightness(frame) bl = blur_score(frame) black = black_ratio(frame) fp = { \"frame_index\": idx, \"timestamp_sec\": round(float(ts), 4), \"phash\": phash(frame), \"dhash\": dhash(frame), \"hsv_histogram\": hsv_hist(frame), \"edge_histogram\": edge_hist(frame), \"brightness\": br, \"blur_score\": bl, \"black_ratio\": black, \"tile_hashes\": tile_hashes(frame), \"frame_diff_mean\": frame_diff(prev, frame), \"optical_flow\": optical_flow_signature(prev, frame), \"quality_score\": quality_score(br, bl, black), } fprints.append(fp) prev = frame tile_keys = sorted(fprints[0][\"tile_hashes\"].keys()) if fprints else [] tile_med = {k: median_hash([x[\"tile_hashes\"][k] for x in fprints]) for k in tile_keys} diffs = [x[\"frame_diff_mean\"] for x in fprints[1:]] flows = [x[\"optical_flow\"][\"flow_mean\"] for x in fprints[1:]] qs = [x[\"quality_score\"] for x in fprints] brs = [x[\"brightness\"] for x in fprints] blurs = [x[\"blur_score\"] for x in fprints] blacks = [x[\"black_ratio\"] for x in fprints] card = VisualFingerprintCard( fingerprint_id=fid, asset_id=aid, scene_id=sid, variant_id=variant_id, source_path=str(p), media_type=str(meta.get(\"media_type\", \"unknown\")), width=int(meta.get(\"width\") or 0), height=int(meta.get(\"height\") or 0), fps=round(float(meta.get(\"fps\") or 0.0), 4), duration_sec=round(float(meta.get(\"duration_sec\") or 0.0), 4), sample_interval_sec=self.interval_sec, sampled_frames=len(fprints), frame_fingerprints=fprints, scene_phash_median=median_hash([x[\"phash\"] for x in fprints]), scene_dhash_median=median_hash([x[\"dhash\"] for x in fprints]), scene_color_hist_avg=avg_vectors([x[\"hsv_histogram\"] for x in fprints]), scene_edge_hist_avg=avg_vectors([x[\"edge_histogram\"] for x in fprints]), tile_hashes_median=tile_med, motion_signature={ \"frame_diff_mean\": round(float(np.mean(diffs)) if diffs else 0.0, 6), \"frame_diff_p95\": round(float(np.percentile(diffs, 95)) if diffs else 0.0, 6), \"flow_mean\": round(float(np.mean(flows)) if flows else 0.0, 6), \"motion_score\": round(float(np.mean(diffs)) if diffs else 0.0, 6), \"static_score\": round(max(0.0, 1.0 - min(1.0, (float(np.mean(diffs)) if diffs else 0.0) * 5)), 6), }, quality={ \"quality_score\": round(float(np.mean(qs)) if qs else 0.0, 3), \"brightness_mean\": round(float(np.mean(brs)) if brs else 0.0, 4), \"blur_score_mean\": round(float(np.mean(blurs)) if blurs else 0.0, 4), \"black_ratio_mean\": round(float(np.mean(blacks)) if blacks else 0.0, 6), }, ) report[\"card\"] = asdict(card) report[\"runtime_ms\"] = now_ms() - start out = self.output_dir / f\"{fid}.visual_fingerprint.json\" dump_json(out, report[\"card\"]) report[\"output\"] = str(out) return report def run(self, ctx: dict[str, Any]) -> dict[str, Any]: assets = ctx.get(\"assets\") or ctx.get(\"clean_assets\") or ctx.get(\"variants\") or [] cards, reports = [], [] for a in assets: if isinstance(a, (str, Path)): path, aid, sid, vid = str(a), None, None, None else: path = a.get(\"path\") or a.get(\"output_path\") or a.get(\"source_path\") aid = a.get(\"asset_id\") or a.get(\"source_asset_id\") sid = a.get(\"scene_id\") vid = a.get(\"variant_id\") if not path: continue r = self.analyze(path, aid, sid, vid) reports.append(r) if r.get(\"card\"): cards.append(r[\"card\"]) summary = {\"engine\": \"visual_fingerprint_engine\", \"status\": \"PASS\" if cards else \"FAILED\", \"card_count\": len(cards), \"reports\": reports} dump_json(self.output_dir / \"visual_fingerprint_summary.json\", summary) ctx[\"visual_fingerprints\"] = cards ctx[\"visual_fingerprint_reports\"] = reports return ctxdef main() -> int: ap = argparse.ArgumentParser() ap.add_argument(\"--input\", required=True) ap.add_argument(\"--output\", default=\"reports/visual_fingerprint\") ap.add_argument(\"--interval\", type=float, default=1.0) ap.add_argument(\"--max-frames\", type=int, default=7200) args = ap.parse_args() engine = VisualFingerprintEngine(args.output, args.interval, args.max_frames) inp = Path(args.input) if inp.is_dir(): ctx = {\"assets\": [str(p) for p in inp.rglob(\"*\") if p.suffix.lower() in IMAGE_EXTS | VIDEO_EXTS]} ctx = engine.run(ctx) print(json.dumps({\"status\": \"PASS\", \"count\": len(ctx.get(\"visual_fingerprints\", []))}, ensure_ascii=False, indent=2)) return 0 if ctx.get(\"visual_fingerprints\") else 1 r = engine.analyze(inp) dump_json(Path(args.output) / \"latest_visual_fingerprint_report.json\", r) print(json.dumps(r, ensure_ascii=False, indent=2)) return 0 if r[\"status\"] == \"PASS\" else 1if __name__ == \"__main__\": raise SystemExit(main())\nPython# audio_fingerprint_engine.pyfrom __future__ import annotationsimport argparseimport hashlibimport jsonimport mathimport osimport shutilimport subprocessimport tempfileimport timeimport wavefrom dataclasses import asdict, dataclass, fieldfrom pathlib import Pathfrom typing import Anyimport numpy as npAUDIO_EXTS = {\".wav\", \".mp3\", \".m4a\", \".aac\", \".flac\", \".ogg\", \".wma\"}VIDEO_EXTS = {\".mp4\", \".mov\", \".mkv\", \".avi\", \".webm\", \".m4v\"}DEFAULT_SR = 16000def now_ms() -> int: return int(time.time() * 1000)def mkdir(path: Path) -> None: path.mkdir(parents=True, exist_ok=True)def dump_json(path: Path, data: Any) -> None: mkdir(path.parent) path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding=\"utf-8\")def sha1_bytes(data: bytes, n: int = 16) -> str: return hashlib.sha1(data).hexdigest()[:n]def sha1_text(text: str, n: int = 16) -> str: return hashlib.sha1(text.encode(\"utf-8\", errors=\"ignore\")).hexdigest()[:n]def audio_id_for(path: Path) -> str: try: st = path.stat() raw = f\"{path.resolve()}:{st.st_size}:{st.st_mtime_ns}\" except Exception: raw = str(path) return \"audio_\" + sha1_text(raw, 12)def find_ffmpeg(root: str | Path | None = None) -> Path | None: candidates = [] if root: r = Path(root) candidates.extend([ r / \"_bundled\" / \"ffmpeg\" / \"bin\" / \"ffmpeg.exe\", r / \"_bundled\" / \"ffmpeg\" / \"ffmpeg.exe\", ]) env = os.environ.get(\"PHS_FFMPEG_BIN\") if env: candidates.append(Path(env) / \"ffmpeg.exe\") which = shutil.which(\"ffmpeg\") if which: candidates.append(Path(which)) for p in candidates: if p.exists(): return p return Nonedef extract_wav(src: Path, dst: Path, sr: int, root: str | Path | None = None) -> tuple[bool, str]: ffmpeg = find_ffmpeg(root) if not ffmpeg: return False, \"ffmpeg_not_found\" cmd = [ str(ffmpeg), \"-y\", \"-i\", str(src), \"-vn\", \"-ac\", \"1\", \"-ar\", str(sr), \"-f\", \"wav\", str(dst), ] try: p = subprocess.run(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True, encoding=\"utf-8\", errors=\"replace\", timeout=300) ok = p.returncode == 0 and dst.exists() and dst.stat().st_size > 44 return ok, p.stdout[-2000:] except Exception as e: return False, repr(e)def read_wav(path: Path) -> tuple[np.ndarray, int]: with wave.open(str(path), \"rb\") as wf: ch = wf.getnchannels() sr = wf.getframerate() sw = wf.getsampwidth() n = wf.getnframes() data = wf.readframes(n) if sw == 1: y = np.frombuffer(data, dtype=np.uint8).astype(np.float32) y = (y - 128.0) / 128.0 elif sw == 2: y = np.frombuffer(data, dtype=np.int16).astype(np.float32) / 32768.0 elif sw == 4: y = np.frombuffer(data, dtype=np.int32).astype(np.float32) / 2147483648.0 else: raise ValueError(f\"unsupported_sample_width:{sw}\") if ch > 1: y = y.reshape(-1, ch).mean(axis=1) return np.clip(y, -1.0, 1.0), int(sr)def load_audio(path: Path, sr: int, root: str | Path | None = None) -> tuple[np.ndarray, int, dict[str, Any]]: meta = {\"input\": str(path), \"sample_rate\": sr, \"converted\": False, \"errors\": []} if path.suffix.lower() == \".wav\": try: y, got_sr = read_wav(path) if got_sr == sr: meta[\"source\"] = \"wav_direct\" return y, got_sr, meta except Exception as e: meta[\"errors\"].append(f\"wav_direct_failed:{repr(e)}\") with tempfile.TemporaryDirectory(prefix=\"phs_audio_fp_\") as td: wav_path = Path(td) / \"audio.wav\" ok, log = extract_wav(path, wav_path, sr, root) meta[\"ffmpeg_log_tail\"] = log if not ok: raise RuntimeError(f\"ffmpeg_extract_failed:{log}\") y, got_sr = read_wav(wav_path) meta[\"source\"] = \"ffmpeg\" meta[\"converted\"] = True return y, got_sr, metadef rms_db(y: np.ndarray) -> float: if y.size == 0: return -120.0 v = float(np.sqrt(np.mean(np.square(y.astype(np.float64)))) + 1e-12) return round(20 * math.log10(v), 4)def peak_db(y: np.ndarray) -> float: if y.size == 0: return -120.0 v = float(np.max(np.abs(y)) + 1e-12) return round(20 * math.log10(v), 4)def zero_crossing_rate(y: np.ndarray) -> float: if y.size < 2: return 0.0 signs = np.signbit(y) return round(float(np.mean(signs[1:] != signs[:-1])), 6)def silence_ratio(y: np.ndarray, threshold: float = 0.012) -> float: if y.size == 0: return 1.0 return round(float(np.mean(np.abs(y) < threshold)), 6)def energy_curve(y: np.ndarray, bins: int = 32) -> list[float]: if y.size == 0: return [0.0] * bins parts = np.array_split(y.astype(np.float32), bins) vals = np.array([float(np.sqrt(np.mean(p * p))) if p.size else 0.0 for p in parts], dtype=np.float32) mx = float(np.max(vals)) if mx > 1e-12: vals = vals / mx return vals.round(6).astype(float).tolist()def vector_hash(vals: list[float], levels: int = 16) -> str: a = np.asarray(vals, dtype=np.float32) if a.size == 0: return \"\" a = np.clip(a, 0, 1) q = np.round(a * (levels - 1)).astype(np.uint8) return sha1_bytes(q.tobytes(), 16)def fft_signature(y: np.ndarray, sr: int, bins: int = 32) -> list[float]: if y.size == 0: return [0.0] * bins n = 2 ** int(math.ceil(math.log2(max(256, min(y.size, sr * 10))))) seg = y[: min(y.size, n)] if seg.size < n: seg = np.pad(seg, (0, n - seg.size)) win = np.hanning(n).astype(np.float32) spec = np.abs(np.fft.rfft(seg * win)) spec = np.log1p(spec) edges = np.linspace(0, spec.size, bins + 1).astype(int) vals = [] for i in range(bins): a, b = edges[i], max(edges[i] + 1, edges[i + 1]) vals.append(float(np.mean(spec[a:b]))) arr = np.asarray(vals, dtype=np.float32) mx = float(np.max(arr)) if mx > 1e-12: arr = arr / mx return arr.round(6).astype(float).tolist()def spectral_stats(y: np.ndarray, sr: int) -> dict[str, float]: if y.size == 0: return {\"centroid\": 0.0, \"rolloff\": 0.0, \"bandwidth\": 0.0} n = 2 ** int(math.ceil(math.log2(max(256, min(y.size, sr * 5))))) seg = y[: min(y.size, n)] if seg.size < n: seg = np.pad(seg, (0, n - seg.size)) mag = np.abs(np.fft.rfft(seg * np.hanning(n))).astype(np.float64) freqs = np.fft.rfftfreq(n, 1.0 / sr) total = float(np.sum(mag) + 1e-12) centroid = float(np.sum(freqs * mag) / total) cumsum = np.cumsum(mag) idx = int(np.searchsorted(cumsum, 0.85 * cumsum[-1])) rolloff = float(freqs[min(idx, len(freqs) - 1)]) bandwidth = float(np.sqrt(np.sum(((freqs - centroid) ** 2) * mag) / total)) return {\"centroid\": round(centroid, 4), \"rolloff\": round(rolloff, 4), \"bandwidth\": round(bandwidth, 4)}def pause_pattern(y: np.ndarray, sr: int, frame_ms: int = 100, threshold: float = 0.012) -> dict[str, Any]: frame = max(1, int(sr * frame_ms / 1000)) states = [] for i in range(0, y.size, frame): seg = y[i:i + frame] e = float(np.sqrt(np.mean(seg * seg))) if seg.size else 0.0 states.append(1 if e >= threshold else 0) if not states: return {\"pause_hash\": \"\", \"speech_ratio\": 0.0, \"pause_count\": 0, \"longest_pause_sec\": 0.0} runs = [] cur, ln = states[0], 1 for s in states[1:]: if s == cur: ln += 1 else: runs.append((cur, ln)) cur, ln = s, 1 runs.append((cur, ln)) pauses = [l for s, l in runs if s == 0] return { \"pause_hash\": sha1_bytes(bytes(states), 16), \"speech_ratio\": round(float(np.mean(states)), 6), \"pause_count\": len(pauses), \"longest_pause_sec\": round((max(pauses) * frame_ms / 1000) if pauses else 0.0, 4), \"pattern\": [f\"{s}:{min(l, 99)}\" for s, l in runs[:512]], }def chunks(y: np.ndarray, sr: int, chunk_sec: float) -> list[tuple[int, float, float, np.ndarray]]: size = max(1, int(sr * chunk_sec)) out = [] for idx, start in enumerate(range(0, y.size, size)): end = min(y.size, start + size) out.append((idx, start / sr, end / sr, y[start:end])) return outdef audio_quality(rms: float, peak: float, silence: float) -> float: score = 100.0 if rms < -40: score -= 35 elif rms < -32: score -= 18 elif rms > -8: score -= 20 if peak > -0.5: score -= 30 elif peak > -1.0: score -= 15 if silence > 0.65: score -= 35 elif silence > 0.35: score -= 15 return round(max(0.0, min(100.0, score)), 3)@dataclassclass AudioFingerprintCard: fingerprint_id: str audio_id: str source_path: str media_type: str duration_sec: float sample_rate: int total_samples: int chunk_sets: dict[str, list[dict[str, Any]]] global_rms_db: float global_peak_db: float global_zero_crossing_rate: float global_silence_ratio: float global_energy_curve: list[float] global_energy_hash: str global_fft_signature: list[float] global_spectral_hash: str speech_rhythm_signature: dict[str, Any] quality: dict[str, Any] created_at_ms: int = field(default_factory=now_ms) engine: str = \"audio_fingerprint_engine\" engine_version: str = \"1.5.1\"class AudioFingerprintEngine: def __init__(self, output_dir: str | Path = \"reports/audio_fingerprint\", sample_rate: int = DEFAULT_SR, root: str | Path | None = None): self.output_dir = Path(output_dir) self.sample_rate = sample_rate self.root = root def chunk_card(self, audio_id: str, idx: int, start: float, end: float, seg: np.ndarray, sr: int, chunk_sec: float) -> dict[str, Any]: ec = energy_curve(seg, 32) fs = fft_signature(seg, sr, 32) stats = spectral_stats(seg, sr) return { \"chunk_id\": f\"{audio_id}_chunk_{int(chunk_sec)}s_{idx:05d}\", \"index\": idx, \"start_sec\": round(start, 4), \"end_sec\": round(end, 4), \"duration_sec\": round(end - start, 4), \"chunk_size_sec\": chunk_sec, \"rms_db\": rms_db(seg), \"peak_db\": peak_db(seg), \"zero_crossing_rate\": zero_crossing_rate(seg), \"silence_ratio\": silence_ratio(seg), \"energy_curve\": ec, \"energy_hash\": vector_hash(ec), \"fft_signature\": fs, \"spectral_hash\": vector_hash(fs), \"spectral_stats\": stats, \"pause_pattern\": pause_pattern(seg, sr), } def analyze(self, path: str | Path, audio_id: str | None = None) -> dict[str, Any]: start = now_ms() p = Path(path) report = {\"status\": \"PASS\", \"path\": str(p), \"card\": None, \"errors\": [], \"runtime_ms\": 0} if not p.exists(): report[\"status\"] = \"FAILED\" report[\"errors\"].append(\"file_missing\") return report if p.suffix.lower() not in AUDIO_EXTS | VIDEO_EXTS: report[\"status\"] = \"FAILED\" report[\"errors\"].append(f\"unsupported_ext:{p.suffix.lower()}\") return report try: y, sr, load_meta = load_audio(p, self.sample_rate, self.root) aid = audio_id or audio_id_for(p) fid = \"af_\" + sha1_text(str(p.resolve()) + str(time.time_ns()), 16) chunk_sets: dict[str, list[dict[str, Any]]] = {} for cs in (2.0, 5.0): chunk_sets[f\"{int(cs)}s\"] = [ self.chunk_card(aid, idx, s, e, seg, sr, cs) for idx, s, e, seg in chunks(y, sr, cs) ] ec = energy_curve(y, 64) fs = fft_signature(y, sr, 64) g_rms = rms_db(y) g_peak = peak_db(y) g_sil = silence_ratio(y) card = AudioFingerprintCard( fingerprint_id=fid, audio_id=aid, source_path=str(p), media_type=\"audio\" if p.suffix.lower() in AUDIO_EXTS else \"video_audio\", duration_sec=round(float(y.size / sr) if sr else 0.0, 4), sample_rate=sr, total_samples=int(y.size), chunk_sets=chunk_sets, global_rms_db=g_rms, global_peak_db=g_peak, global_zero_crossing_rate=zero_crossing_rate(y), global_silence_ratio=g_sil, global_energy_curve=ec, global_energy_hash=vector_hash(ec), global_fft_signature=fs, global_spectral_hash=vector_hash(fs), speech_rhythm_signature=pause_pattern(y, sr), quality={ \"audio_quality_score\": audio_quality(g_rms, g_peak, g_sil), \"loudness_ok\": -28.0 <= g_rms <= -10.0, \"has_clipping_risk\": g_peak > -0.5, \"silence_ratio\": g_sil, \"load_meta\": load_meta, }, ) report[\"card\"] = asdict(card) out = self.output_dir / f\"{fid}.audio_fingerprint.json\" dump_json(out, report[\"card\"]) report[\"output\"] = str(out) except Exception as e: report[\"status\"] = \"FAILED\" report[\"errors\"].append(repr(e)) report[\"runtime_ms\"] = now_ms() - start return report def run(self, ctx: dict[str, Any]) -> dict[str, Any]: assets = ctx.get(\"audio_assets\") or ctx.get(\"audios\") or ctx.get(\"tts_outputs\") or ctx.get(\"assets\") or [] cards, reports = [], [] for a in assets: if isinstance(a, (str, Path)): path, aid = str(a), None else: path = a.get(\"path\") or a.get(\"output_path\") or a.get(\"audio_path\") aid = a.get(\"audio_id\") or a.get(\"asset_id\") if not path or Path(path).suffix.lower() not in AUDIO_EXTS | VIDEO_EXTS: continue r = self.analyze(path, aid) reports.append(r) if r.get(\"card\"): cards.append(r[\"card\"]) summary = {\"engine\": \"audio_fingerprint_engine\", \"status\": \"PASS\" if cards else \"FAILED\", \"card_count\": len(cards), \"reports\": reports} dump_json(self.output_dir / \"audio_fingerprint_summary.json\", summary) ctx[\"audio_fingerprints\"] = cards ctx[\"audio_fingerprint_reports\"] = reports return ctxdef main() -> int: ap = argparse.ArgumentParser() ap.add_argument(\"--input\", required=True) ap.add_argument(\"--output\", default=\"reports/audio_fingerprint\") ap.add_argument(\"--sample-rate\", type=int, default=DEFAULT_SR) ap.add_argument(\"--root\", default=None) args = ap.parse_args() engine = AudioFingerprintEngine(args.output, args.sample_rate, args.root) inp = Path(args.input) if inp.is_dir(): ctx = {\"audio_assets\": [str(p) for p in inp.rglob(\"*\") if p.suffix.lower() in AUDIO_EXTS | VIDEO_EXTS]} ctx = engine.run(ctx) print(json.dumps({\"status\": \"PASS\", \"count\": len(ctx.get(\"audio_fingerprints\", []))}, ensure_ascii=False, indent=2)) return 0 if ctx.get(\"audio_fingerprints\") else 1 r = engine.analyze(inp) dump_json(Path(args.output) / \"latest_audio_fingerprint_report.json\", r) print(json.dumps(r, ensure_ascii=False, indent=2)) return 0 if r[\"status\"] == \"PASS\" else 1if __name__ == \"__main__\": raise SystemExit(main())\nPython# de_repeat_planner.pyfrom __future__ import annotationsimport argparseimport hashlibimport jsonimport mathimport randomimport sqlite3import timefrom dataclasses import asdict, dataclassfrom pathlib import Pathfrom typing import AnyDEFAULT_DB = \"runtime/content_identity/content_identity.db\"def now_ms() -> int: return int(time.time() * 1000)def mkdir(path: Path) -> None: path.mkdir(parents=True, exist_ok=True)def dump_json(path: Path, data: Any) -> None: mkdir(path.parent) path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding=\"utf-8\")def sha1_text(text: str, n: int = 16) -> str: return hashlib.sha1(text.encode(\"utf-8\", errors=\"ignore\")).hexdigest()[:n]def hamming_hex(a: str | None, b: str | None) -> int: if not a or not b: return 999999 try: n = min(len(a), len(b)) return bin(int(a[:n], 16) ^ int(b[:n], 16)).count(\"1\") except Exception: return 999999def hash_sim(a: str | None, b: str | None) -> float: if not a or not b: return 0.0 bits = min(len(a), len(b)) * 4 if bits <= 0: return 0.0 return max(0.0, min(1.0, 1.0 - hamming_hex(a, b) / bits))def cos_sim(a: list[float] | None, b: list[float] | None) -> float: if not a or not b: return 0.0 n = min(len(a), len(b)) if n <= 0: return 0.0 x = [float(v) for v in a[:n]] y = [float(v) for v in b[:n]] dot = sum(i * j for i, j in zip(x, y)) nx = math.sqrt(sum(i * i for i in x)) ny = math.sqrt(sum(j * j for j in y)) if nx <= 1e-12 or ny <= 1e-12: return 0.0 return max(0.0, min(1.0, dot / (nx * ny)))def l2_sim(a: list[float] | None, b: list[float] | None) -> float: if not a or not b: return 0.0 n = min(len(a), len(b)) if n <= 0: return 0.0 d = math.sqrt(sum((float(a[i]) - float(b[i])) ** 2 for i in range(n)) / n) return max(0.0, min(1.0, 1.0 - d))def visual_similarity(a: dict[str, Any], b: dict[str, Any]) -> float: ph = hash_sim(a.get(\"scene_phash_median\"), b.get(\"scene_phash_median\")) dh = hash_sim(a.get(\"scene_dhash_median\"), b.get(\"scene_dhash_median\")) hist = cos_sim(a.get(\"scene_color_hist_avg\"), b.get(\"scene_color_hist_avg\")) edge = cos_sim(a.get(\"scene_edge_hist_avg\"), b.get(\"scene_edge_hist_avg\")) ta = a.get(\"tile_hashes_median\") or {} tb = b.get(\"tile_hashes_median\") or {} tile_scores = [hash_sim(ta[k], tb[k]) for k in ta.keys() if k in tb] tile = sum(tile_scores) / len(tile_scores) if tile_scores else 0.0 ma = float((a.get(\"motion_signature\") or {}).get(\"motion_score\", 0.0)) mb = float((b.get(\"motion_signature\") or {}).get(\"motion_score\", 0.0)) motion = max(0.0, 1.0 - abs(ma - mb)) return round(ph * 0.30 + dh * 0.10 + hist * 0.22 + edge * 0.10 + tile * 0.20 + motion * 0.08, 6)def audio_chunk_similarity(a: dict[str, Any], b: dict[str, Any]) -> float: energy = l2_sim(a.get(\"energy_curve\"), b.get(\"energy_curve\")) spectral = cos_sim(a.get(\"fft_signature\"), b.get(\"fft_signature\")) pause = hash_sim((a.get(\"pause_pattern\") or {}).get(\"pause_hash\"), (b.get(\"pause_pattern\") or {}).get(\"pause_hash\")) return round(energy * 0.38 + spectral * 0.42 + pause * 0.20, 6)def load_json_cards(paths: list[str | Path]) -> list[dict[str, Any]]: cards = [] for p0 in paths: p = Path(p0) files = list(p.rglob(\"*.json\")) if p.is_dir() else [p] for f in files: try: data = json.loads(f.read_text(encoding=\"utf-8\")) except Exception: continue if isinstance(data, dict) and \"fingerprint_id\" in data: cards.append(data) elif isinstance(data, dict) and isinstance(data.get(\"card\"), dict): cards.append(data[\"card\"]) elif isinstance(data, dict) and isinstance(data.get(\"reports\"), list): for r in data[\"reports\"]: if isinstance(r, dict) and isinstance(r.get(\"card\"), dict): cards.append(r[\"card\"]) elif isinstance(data, list): cards.extend([x for x in data if isinstance(x, dict) and \"fingerprint_id\" in x]) return cards@dataclassclass Constraints: same_scene_min_gap_sec: int = 180 same_asset_min_gap_sec: int = 90 same_variant_min_gap_sec: int = 180 high_visual_similarity_threshold: float = 0.88 max_adjacent_visual_similarity: float = 0.72 same_audio_chunk_min_gap_sec: int = 300 high_audio_similarity_threshold: float = 0.88 max_adjacent_audio_similarity: float = 0.75 default_visual_segment_duration_sec: float = 6.0class FingerprintStore: def __init__(self, db_path: str | Path = DEFAULT_DB): self.db_path = Path(db_path) mkdir(self.db_path.parent) self.conn = sqlite3.connect(str(self.db_path)) self.conn.row_factory = sqlite3.Row self.create_tables() def create_tables(self) -> None: self.conn.executescript( \"\"\" CREATE TABLE IF NOT EXISTS visual_fingerprints ( fingerprint_id TEXT PRIMARY KEY, asset_id TEXT, scene_id TEXT, variant_id TEXT, duration_sec REAL, quality_score REAL, card_json TEXT, created_at_ms INTEGER ); CREATE TABLE IF NOT EXISTS audio_chunks ( chunk_id TEXT PRIMARY KEY, audio_id TEXT, fingerprint_id TEXT, start_sec REAL, end_sec REAL, duration_sec REAL, quality_score REAL, card_json TEXT, created_at_ms INTEGER ); CREATE TABLE IF NOT EXISTS similarity_edges ( edge_id TEXT PRIMARY KEY, left_id TEXT, right_id TEXT, modality TEXT, similarity REAL, block_adjacent INTEGER, min_gap_sec REAL, created_at_ms INTEGER ); CREATE TABLE IF NOT EXISTS generation_runs ( generation_id TEXT PRIMARY KEY, run_seed INTEGER, target_duration_sec REAL, visual_sequence_hash TEXT, repeat_risk_score REAL, created_at_ms INTEGER ); CREATE TABLE IF NOT EXISTS generation_usage ( usage_id TEXT PRIMARY KEY, generation_id TEXT, modality TEXT, item_id TEXT, source_id TEXT, start_sec REAL, duration_sec REAL, created_at_ms INTEGER ); \"\"\" ) self.conn.commit() def upsert_visual(self, card: dict[str, Any]) -> None: q = float((card.get(\"quality\") or {}).get(\"quality_score\", 0.0)) self.conn.execute( \"\"\" INSERT OR REPLACE INTO visual_fingerprints VALUES (?, ?, ?, ?, ?, ?, ?, ?) \"\"\", ( card.get(\"fingerprint_id\"), card.get(\"asset_id\"), card.get(\"scene_id\"), card.get(\"variant_id\"), float(card.get(\"duration_sec\") or 0.0), q, json.dumps(card, ensure_ascii=False), now_ms(), ), ) self.conn.commit() def upsert_audio(self, card: dict[str, Any]) -> None: chunk_sets = card.get(\"chunk_sets\") or {} chunks = chunk_sets.get(\"5s\") or chunk_sets.get(\"2s\") or [] for ch in chunks: q = self.audio_chunk_quality(ch) self.conn.execute( \"\"\" INSERT OR REPLACE INTO audio_chunks VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?) \"\"\", ( ch.get(\"chunk_id\"), card.get(\"audio_id\"), card.get(\"fingerprint_id\"), float(ch.get(\"start_sec\") or 0.0), float(ch.get(\"end_sec\") or 0.0), float(ch.get(\"duration_sec\") or 0.0), q, json.dumps(ch, ensure_ascii=False), now_ms(), ), ) self.conn.commit() @staticmethod def audio_chunk_quality(ch: dict[str, Any]) -> float: score = 100.0 if float(ch.get(\"silence_ratio\", 0.0)) > 0.70: score -= 35 if float(ch.get(\"peak_db\", -120.0)) > -0.5: score -= 25 if float(ch.get(\"rms_db\", -120.0)) < -38: score -= 20 return max(0.0, min(100.0, score)) def load_visual(self) -> list[dict[str, Any]]: rows = self.conn.execute(\"SELECT card_json FROM visual_fingerprints\").fetchall() return [json.loads(r[\"card_json\"]) for r in rows] def load_audio_chunks(self) -> list[dict[str, Any]]: rows = self.conn.execute(\"SELECT card_json, audio_id, fingerprint_id FROM audio_chunks ORDER BY audio_id, start_sec\").fetchall() out = [] for r in rows: ch = json.loads(r[\"card_json\"]) ch[\"audio_id\"] = r[\"audio_id\"] ch[\"fingerprint_id\"] = r[\"fingerprint_id\"] out.append(ch) return out def recent_usage(self, modality: str, runs: int = 10) -> dict[str, int]: rows = self.conn.execute( \"\"\" SELECT item_id, COUNT(*) c FROM generation_usage WHERE modality=? GROUP BY item_id \"\"\", (modality,), ).fetchall() return {r[\"item_id\"]: int(r[\"c\"]) for r in rows} def save_run(self, generation_id: str, run_seed: int, target: float, visual_timeline: list[dict[str, Any]], risk: float) -> None: seq_hash = sha1_text(json.dumps([(x[\"fingerprint_id\"], x[\"start_sec\"]) for x in visual_timeline], sort_keys=True), 20) self.conn.execute( \"\"\" INSERT OR REPLACE INTO generation_runs VALUES (?, ?, ?, ?, ?, ?) \"\"\", (generation_id, run_seed, target, seq_hash, risk, now_ms()), ) for item in visual_timeline: usage_id = \"usage_\" + sha1_text(generation_id + item[\"id\"] + str(item[\"start_sec\"]), 20) self.conn.execute( \"\"\" INSERT OR REPLACE INTO generation_usage VALUES (?, ?, ?, ?, ?, ?, ?, ?) \"\"\", ( usage_id, generation_id, \"visual\", item[\"fingerprint_id\"], item.get(\"scene_id\") or item.get(\"source_id\"), float(item[\"start_sec\"]), float(item[\"duration_sec\"]), now_ms(), ), ) self.conn.commit()class DeRepeatPlanner: def __init__(self, store: FingerprintStore, constraints: Constraints | None = None, run_seed: int | None = None): self.store = store self.constraints = constraints or Constraints() self.run_seed = run_seed if run_seed is not None else random.randint(1, 2_000_000_000) self.rng = random.Random(self.run_seed) def allowed( self, cand: dict[str, Any], last: dict[str, Any] | None, now_sec: float, use_scene: dict[str, list[float]], use_asset: dict[str, list[float]], use_variant: dict[str, list[float]], ) -> tuple[bool, list[str], float]: reasons = [] sim_last = 0.0 sid = cand.get(\"scene_id\") or cand[\"fingerprint_id\"] aid = cand.get(\"asset_id\") or cand[\"fingerprint_id\"] vid = cand.get(\"variant_id\") or cand[\"fingerprint_id\"] if last: last_sid = last.get(\"scene_id\") or last[\"fingerprint_id\"] if sid == last_sid: return False, [\"same_scene_adjacent_blocked\"], 1.0 sim_last = visual_similarity(cand, last) if sim_last >= self.constraints.high_visual_similarity_threshold: return False, [\"high_visual_similarity_adjacent_blocked\"], sim_last if sim_last >= self.constraints.max_adjacent_visual_similarity: reasons.append(\"adjacent_similarity_penalty\") if sid in use_scene and now_sec - use_scene[sid][-1] < self.constraints.same_scene_min_gap_sec: return False, [\"same_scene_min_gap_blocked\"], sim_last if aid in use_asset and now_sec - use_asset[aid][-1] < self.constraints.same_asset_min_gap_sec: return False, [\"same_asset_min_gap_blocked\"], sim_last if vid in use_variant and now_sec - use_variant[vid][-1] < self.constraints.same_variant_min_gap_sec: return False, [\"same_variant_min_gap_blocked\"], sim_last return True, reasons, sim_last def plan_visual(self, target_duration_sec: float) -> tuple[list[dict[str, Any]], dict[str, Any]]: cards = self.store.load_visual() recent = self.store.recent_usage(\"visual\") if not cards: return [], {\"status\": \"FAILED\", \"reason\": \"no_visual_fingerprints\"} cards = sorted(cards, key=lambda c: float((c.get(\"quality\") or {}).get(\"quality_score\", 0.0)), reverse=True) timeline = [] use_scene: dict[str, list[float]] = {} use_asset: dict[str, list[float]] = {} use_variant: dict[str, list[float]] = {} t = 0.0 last = None attempts = 0 max_attempts = max(100, int(target_duration_sec * 20)) while t < target_duration_sec and attempts < max_attempts: attempts += 1 ranked = [] for c in cards: ok, reasons, sim_last = self.allowed(c, last, t, use_scene, use_asset, use_variant) if not ok: continue q = float((c.get(\"quality\") or {}).get(\"quality_score\", 0.0)) diversity = self.rng.uniform(0, 12) recent_penalty = min(30.0, recent.get(c[\"fingerprint_id\"], 0) * 8.0) similarity_penalty = sim_last * 45.0 underused = max(0.0, 10.0 - len(use_scene.get(c.get(\"scene_id\") or c[\"fingerprint_id\"], [])) * 2.5) score = q + diversity + underused - similarity_penalty - recent_penalty ranked.append((score, c, reasons, sim_last)) if not ranked: for c in cards: q = float((c.get(\"quality\") or {}).get(\"quality_score\", 0.0)) score = q + self.rng.uniform(0, 8) - min(20.0, recent.get(c[\"fingerprint_id\"], 0) * 6) ranked.append((score, c, [\"relaxed_constraints\"], 0.0)) ranked.sort(key=lambda x: x[0], reverse=True) score, c, reasons, sim_last = ranked[0] base_dur = float(c.get(\"duration_sec\") or 0.0) dur = base_dur if base_dur > 0 else self.constraints.default_visual_segment_duration_sec dur = min(max(3.0, min(dur, 12.0)), target_duration_sec - t) item = { \"id\": f\"visual_{len(timeline):05d}\", \"start_sec\": round(t, 4), \"duration_sec\": round(dur, 4), \"source_id\": c.get(\"source_path\") or c[\"fingerprint_id\"], \"fingerprint_id\": c[\"fingerprint_id\"], \"asset_id\": c.get(\"asset_id\"), \"scene_id\": c.get(\"scene_id\"), \"variant_id\": c.get(\"variant_id\"), \"score\": round(float(score), 4), \"reason\": reasons + [f\"sim_to_last:{round(sim_last, 4)}\"], } timeline.append(item) sid = c.get(\"scene_id\") or c[\"fingerprint_id\"] aid = c.get(\"asset_id\") or c[\"fingerprint_id\"] vid = c.get(\"variant_id\") or c[\"fingerprint_id\"] use_scene.setdefault(sid, []).append(t) use_asset.setdefault(aid, []).append(t) use_variant.setdefault(vid, []).append(t) last = c t += dur risk = self.repeat_risk(timeline) return timeline, { \"status\": \"PASS\" if timeline else \"FAILED\", \"target_duration_sec\": target_duration_sec, \"actual_duration_sec\": round(sum(x[\"duration_sec\"] for x in timeline), 4), \"items\": len(timeline), \"attempts\": attempts, \"visual_repeat_risk_score\": risk, } @staticmethod def repeat_risk(timeline: list[dict[str, Any]]) -> float: if len(timeline) <= 1: return 0.0 same_scene_adj = 0 same_asset_adj = 0 reuse: dict[str, int] = {} for i in range(1, len(timeline)): if timeline[i].get(\"scene_id\") == timeline[i - 1].get(\"scene_id\"): same_scene_adj += 1 if timeline[i].get(\"asset_id\") == timeline[i - 1].get(\"asset_id\"): same_asset_adj += 1 for x in timeline: fp = x[\"fingerprint_id\"] reuse[fp] = reuse.get(fp, 0) + 1 reuse_score = sum(max(0, c - 1) for c in reuse.values()) / max(1, len(timeline)) adj_score = (same_scene_adj + same_asset_adj * 0.5) / max(1, len(timeline) - 1) return round(max(0.0, min(1.0, reuse_score * 0.7 + adj_score * 0.3)), 6) def make_plan(self, target_duration_sec: float, output_dir: str | Path) -> dict[str, Any]: out = Path(output_dir) mkdir(out) visual_timeline, visual_report = self.plan_visual(target_duration_sec) generation_id = f\"gen_{int(time.time())}_{sha1_text(str(self.run_seed), 8)}\" sequence_hash = sha1_text(json.dumps([(x[\"fingerprint_id\"], x[\"start_sec\"]) for x in visual_timeline], sort_keys=True), 20) risk = float(visual_report.get(\"visual_repeat_risk_score\", 0.0)) plan = { \"engine\": \"de_repeat_planner\", \"engine_version\": \"1.5.1\", \"generation_id\": generation_id, \"run_seed\": self.run_seed, \"target_duration_sec\": target_duration_sec, \"constraints\": asdict(self.constraints), \"visual_report\": visual_report, \"visual_timeline\": visual_timeline, \"visual_sequence_hash\": sequence_hash, \"repeat_risk_score\": risk, \"status\": \"PASS\" if visual_timeline else \"FAILED\", \"created_at_ms\": now_ms(), } dump_json(out / \"dedup_plan.json\", plan) dump_json(out / \"visual_timeline.json\", visual_timeline) dump_json(out / \"repeat_risk_report.json\", { \"generation_id\": generation_id, \"visual_repeat_risk_score\": risk, \"visual_sequence_hash\": sequence_hash, }) if visual_timeline: self.store.save_run(generation_id, self.run_seed, target_duration_sec, visual_timeline, risk) return planclass DeRepeatPlannerEngine: def __init__(self, db_path: str | Path = DEFAULT_DB, output_dir: str | Path = \"reports/de_repeat_planner\"): self.store = FingerprintStore(db_path) self.output_dir = Path(output_dir) def ingest(self, visual_cards: list[dict[str, Any]], audio_cards: list[dict[str, Any]] | None = None) -> None: for c in visual_cards: self.store.upsert_visual(c) for c in audio_cards or []: self.store.upsert_audio(c) def run(self, ctx: dict[str, Any]) -> dict[str, Any]: visual_cards = ctx.get(\"visual_fingerprints\") or [] audio_cards = ctx.get(\"audio_fingerprints\") or [] target = float(ctx.get(\"target_duration_sec\") or ctx.get(\"target_duration\") or 1800) run_seed = ctx.get(\"run_seed\") self.ingest(visual_cards, audio_cards) planner = DeRepeatPlanner(self.store, run_seed=run_seed) plan = planner.make_plan(target, self.output_dir) ctx[\"dedup_plan\"] = plan ctx[\"visual_timeline\"] = plan.get(\"visual_timeline\", []) return ctxdef main() -> int: ap = argparse.ArgumentParser() ap.add_argument(\"--db\", default=DEFAULT_DB) ap.add_argument(\"--output\", default=\"reports/de_repeat_planner\") ap.add_argument(\"--visual\", nargs=\"*\", default=[]) ap.add_argument(\"--audio\", nargs=\"*\", default=[]) ap.add_argument(\"--target-duration\", type=float, default=1800.0) ap.add_argument(\"--run-seed\", type=int, default=None) args = ap.parse_args() visual_cards = load_json_cards(args.visual) audio_cards = load_json_cards(args.audio) engine = DeRepeatPlannerEngine(args.db, args.output) engine.ingest(visual_cards, audio_cards) planner = DeRepeatPlanner(engine.store, run_seed=args.run_seed) plan = planner.make_plan(args.target_duration, args.output) print(json.dumps({ \"status\": plan[\"status\"], \"generation_id\": plan[\"generation_id\"], \"visual_items\": len(plan.get(\"visual_timeline\", [])), \"repeat_risk_score\": plan.get(\"repeat_risk_score\"), \"output\": args.output, }, ensure_ascii=False, indent=2)) return 0 if plan[\"status\"] == \"PASS\" else 1if __name__ == \"__main__\": raise SystemExit(main())"
}
