{
  "timestamp": "2026-06-28T03:26:39.9359925+08:00",
  "response": "'; function isVisible(el) { return !!(el && (el.offsetWidth || el.offsetHeight || el.getClientRects().length)); } function cleanArticleText(article) { const clone = article.cloneNode(true); clone.querySelectorAll([ 'script', 'style', 'noscript', 'button', 'svg', '[aria-hidden=\"true\"]', '[role=\"button\"]', '[data-testid*=\"copy\"]', '[data-testid*=\"feedback\"]', ].join(',')).forEach(e => e.remove()); let text = (clone.innerText || clone.textContent || '') .replace(/\\u00a0/g, ' ') .replace(/[ \\t]{2,}/g, ' ') .replace(/\\n{3,}/g, '\\n\\n') .trim(); const idx = text.indexOf(marker); if (idx >= 0) text = text.slice(idx + marker.length).trim(); return text; } function isAssistantArticle(article) { const testId = article.getAttribute('data-testid') || ''; const authorRole = article.getAttribute('data-message-author-role') || ''; const text = article.textContent || ''; if (authorRole === 'assistant') return true; if (/assistant/i.test(testId)) return true; if (/conversation-turn-\\d+/.test(testId) && text.includes(marker)) return true; if (text.includes(marker)) return true; return false; } const articles = Array.from(document.querySelectorAll('article[data-testid]')) .filter(isVisible) .filter(isAssistantArticle) .map((article, index) => ({ index, testId: article.getAttribute('data-testid') || '', text: cleanArticleText(article), })) .filter(x => x.text.length > 0); if (articles.length > base.assistantArticleCountBefore) { const added = articles.slice(base.assistantArticleCountBefore); return { text: added[added.length - 1]?.text || '', source: 'article[data-testid]-new-node', assistantArticleCount: articles.length, }; } if (articles.length > 0) { const last = articles[articles.length - 1]; if ( articles.length >= base.assistantArticleCountBefore && last.text && last.text !== base.lastAssistantTextBefore ) { return { text: last.text, source: 'article[data-testid]-mutated-last-node', assistantArticleCount: articles.length, }; } } const main = document.querySelector('main') || document.body; const clone = main.cloneNode(true); clone.querySelectorAll('script,style,noscript,button,svg,[aria-hidden=\"true\"],[role=\"button\"]').forEach(e => e.remove()); const fullText = (clone.innerText || clone.textContent || '') .replace(/\\u00a0/g, ' ') .replace(/[ \\t]{2,}/g, ' ') .replace(/\\n{3,}/g, '\\n\\n') .trim(); const parts = fullText.split(marker); if (parts.length - 1 > base.markerCountBefore) { return { text: parts[parts.length - 1].trim(), source: 'markerCountBefore-fallback', assistantArticleCount: articles.length, }; } return { text: '', source: 'none', assistantArticleCount: articles.length, }; }, baseline);}async function getTargetAssistantArticleLocator(baseline) { const articles = page.locator('article[data-testid]'); const count = await articles.count().catch(() => 0); if (count === 0) return null; // 取新增节点；如果没有新增，取最后一个节点。 const index = count > baseline.assistantArticleCountBefore ? baseline.assistantArticleCountBefore : count - 1; return articles.nth(index);}async function downloadArtifactsFromLatestAssistant(reqId, baseline) { const artifactDir = path.join(ARTIFACTS_DIR, safeFileStem(reqId)); fs.mkdirSync(artifactDir, { recursive: true }); const article = await getTargetAssistantArticleLocator(baseline); if (!article) return []; const links = article.locator('a'); const linkCount = await links.count().catch(() => 0); const artifacts = []; for (let i = 0; i < linkCount; i++) { const link = links.nth(i); const info = await link.evaluate(a => ({ text: (a.innerText || a.textContent || '').trim(), href: a.href || '', download: a.getAttribute('download') || '', })).catch(() => null); if (!info) continue; const looksDownloadable = Boolean(info.download) || /\\.(zip|js|json|md|txt|pdf|docx|xlsx|pptx|csv|png|jpg|jpeg|webp)$/i.test(info.text) || /\\.(zip|js|json|md|txt|pdf|docx|xlsx|pptx|csv|png|jpg|jpeg|webp)(\\?|#|$)/i.test(info.href); if (!looksDownloadable) continue; try { const [download] = await Promise.all([ page.waitForEvent('download', { timeout: 30000 }), link.click({ timeout: 5000 }), ]); const suggested = download.suggestedFilename(); const filename = safeFileStem(suggested || info.download || info.text || `artifact-${i}`); const savePath = path.join(artifactDir, filename); await download.saveAs(savePath); artifacts.push({ ok: true, mode: 'ui_download', name: filename, path: savePath, source_text: info.text, href: redactString(info.href), }); console.log('[artifact] Downloaded:', savePath); } catch (e) { console.log('[artifact] Download failed:', redactString(e.message)); artifacts.push({ ok: false, mode: 'ui_download', name: info.text || `artifact-${i}`, href: redactString(info.href), error: redactString(e.message), }); } } return artifacts;}function extractJsonCandidates(text) { const candidates = []; const fenced = [...String(text || '').matchAll(/```(?:json)?\\s*([\\s\\S]*?)```/gi)]; for (const match of fenced) { candidates.push(match[1].trim()); } const raw = String(text || ''); const firstBrace = raw.indexOf('{'); const lastBrace = raw.lastIndexOf('}'); if (firstBrace !== -1 && lastBrace > firstBrace) { candidates.push(raw.slice(firstBrace, lastBrace + 1)); } return candidates;}function tryParseArtifactSpec(responseText) { for (const candidate of extractJsonCandidates(responseText)) { try { const parsed = JSON.parse(candidate); if (parsed && Array.isArray(parsed.files)) return parsed; } catch {} } return null;}function createZipIfPossible(sourceDir, zipPath) { try { if (process.platform === 'win32') { const ps = [ '-NoProfile', '-ExecutionPolicy', 'Bypass', '-Command', `Compress-Archive -Path \"${sourceDir.replace(/\"/g, '`\"')}\\\\*\" -DestinationPath \"${zipPath.replace(/\"/g, '`\"')}\" -Force`, ]; const res = childProcess.spawnSync('powershell.exe', ps, { encoding: 'utf8' }); if (res.status === 0 && fs.existsSync(zipPath)) return true; console.log('[artifact] Compress-Archive failed:', redactString(res.stderr || res.stdout || 'unknown')); return false; } const res = childProcess.spawnSync('zip', ['-r', zipPath, '.'], { cwd: sourceDir, encoding: 'utf8', }); if (res.status === 0 && fs.existsSync(zipPath)) return true; console.log('[artifact] zip failed:', redactString(res.stderr || res.stdout || 'unknown')); return false; } catch (e) { console.log('[artifact] zip exception:', redactString(e.message)); return false; }}function buildLocalArtifactsFromResponse(reqId, responseText) { const spec = tryParseArtifactSpec(responseText); if (!spec) return []; const artifactDir = path.join(ARTIFACTS_DIR, safeFileStem(reqId)); const filesDir = path.join(artifactDir, 'files'); fs.mkdirSync(filesDir, { recursive: true }); const artifacts = []; for (const file of spec.files) { const rel = safeRelativePath(file.path); if (!rel) { artifacts.push({ ok: false, mode: 'local_build', name: String(file.path || ''), error: 'Invalid relative path', }); continue; } const content = typeof file.content === 'string' ? file.content : ''; const outPath = path.join(filesDir, ...rel.split('/')); atomicWriteFileSync(outPath, content, 'utf8'); artifacts.push({ ok: true, mode: 'local_build', name: rel, path: outPath, bytes: Buffer.byteLength(content, 'utf8'), }); } const zipName = safeFileStem(spec.zip?.name || `${safeFileStem(reqId)}-artifacts.zip`); const zipPath = path.join(artifactDir, zipName); if (artifacts.some(a => a.ok)) { const zipped = createZipIfPossible(filesDir, zipPath); artifacts.push({ ok: zipped, mode: 'local_build', name: zipName, path: zipped ? zipPath : null, error: zipped ? null : 'Zip creation failed; files were still written individually.', }); } atomicWriteJsonSync(path.join(artifactDir, 'manifest.json'), { id: reqId, ts: nowIso(), mode: 'local_build', artifacts: redactDeep(artifacts), }); return artifacts;}async function isGenerating() { return await page.evaluate(() => { const texts = Array.from(document.querySelectorAll('button, [role=\"button\"]')) .map(el => `${el.getAttribute('aria-label') || ''} ${el.textContent || ''}`.trim()); return texts.some(t => /停止|Stop|停止生成|stop generating/i.test(t)); }).catch(() => false);}function cleanupResponse(text) { return String(text || '') .replace(/"
}
