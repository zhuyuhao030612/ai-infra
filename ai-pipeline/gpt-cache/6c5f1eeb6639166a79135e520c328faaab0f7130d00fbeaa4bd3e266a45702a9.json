{
  "timestamp": "2026-06-30T13:30:14.6888944+08:00",
  "response": "PowerShellfunction Invoke-OllamaExtraction { param( [Parameter(Mandatory)][string]$Prompt ) # 修复点： # 1. 不再使用 Start-Process -RedirectStandardOutput，避免 Windows 默认编码导致中文损坏。 # 2. 改用 System.Diagnostics.Process，并显式指定 UTF-8 输入/输出/错误流编码。 # 3. 同时把 stdout/stderr 写入 UTF-8 临时文件，便于排查 Ollama 原始输出。 # 4. 保留超时控制，超时后杀掉 ollama 子进程，但不影响 Ollama 服务本身。 $oldOutputEncoding = $OutputEncoding $oldConsoleOutputEncoding = [Console]::OutputEncoding $oldConsoleInputEncoding = [Console]::InputEncoding $tempOut = [System.IO.Path]::GetTempFileName() $tempErr = [System.IO.Path]::GetTempFileName() try { $utf8NoBom = New-Object System.Text.UTF8Encoding($false) # PowerShell 管道和 Console 编码都切到 UTF-8，避免中文 JSON 被破坏。 $script:OutputEncoding = $utf8NoBom [Console]::OutputEncoding = $utf8NoBom [Console]::InputEncoding = $utf8NoBom $psi = New-Object System.Diagnostics.ProcessStartInfo $psi.FileName = \"ollama\" $psi.UseShellExecute = $false $psi.RedirectStandardInput = $true $psi.RedirectStandardOutput = $true $psi.RedirectStandardError = $true $psi.CreateNoWindow = $true # PowerShell 7 / .NET 支持显式指定标准流编码。 $psi.StandardOutputEncoding = $utf8NoBom $psi.StandardErrorEncoding = $utf8NoBom if ($psi.PSObject.Properties.Name -contains \"StandardInputEncoding\") { $psi.StandardInputEncoding = $utf8NoBom } [void]$psi.ArgumentList.Add(\"run\") [void]$psi.ArgumentList.Add($Model) $process = New-Object System.Diagnostics.Process $process.StartInfo = $psi [void]$process.Start() # 用 UTF-8 stdin 直接把提示词喂给 ollama。 $process.StandardInput.Write($Prompt) $process.StandardInput.Close() # 异步读取，避免 stdout/stderr 缓冲区塞满导致死锁。 $stdoutTask = $process.StandardOutput.ReadToEndAsync() $stderrTask = $process.StandardError.ReadToEndAsync() $completed = $process.WaitForExit($OllamaTimeoutSeconds * 1000) if (-not $completed) { try { $process.Kill($true) } catch { try { $process.Kill() } catch { # 杀进程失败不再抛出，下面统一报超时。 } } throw \"Ollama 调用超时，超过 $OllamaTimeoutSeconds 秒。\" } $stdout = $stdoutTask.GetAwaiter().GetResult() $stderr = $stderrTask.GetAwaiter().GetResult() # 写入 UTF-8 临时文件，方便排查中文 JSON 原始输出。 [System.IO.File]::WriteAllText($tempOut, $stdout, $utf8NoBom) [System.IO.File]::WriteAllText($tempErr, $stderr, $utf8NoBom) if ($process.ExitCode -ne 0) { throw \"Ollama 退出码 $($process.ExitCode)：$stderr\" } if ([string]::IsNullOrWhiteSpace($stdout)) { throw \"Ollama 没有返回内容。\" } # 从 UTF-8 文件读回，确保后续 JSON 解析拿到的是正确中文。 return [System.IO.File]::ReadAllText($tempOut, $utf8NoBom) } finally { # 恢复调用前的编码设置，避免影响外层 PowerShell 会话。 $script:OutputEncoding = $oldOutputEncoding [Console]::OutputEncoding = $oldConsoleOutputEncoding [Console]::InputEncoding = $oldConsoleInputEncoding Remove-Item -Path $tempOut, $tempErr -Force -ErrorAction SilentlyContinue }}function Get-NewRawMarkdownFiles { param( [Parameter(Mandatory)][hashtable]$State ) # 修复点： # 原来只扫描： # 00-inbox\\yyyy-MM-dd\\raw-pages\\*.md # # 现在同时扫描： # 00-inbox\\yyyy-MM-dd\\*.md # 00-inbox\\yyyy-MM-dd\\raw-pages\\*.md # # 仍然用文件 SHA256 做去重，避免同一文件被重复处理。 $files = New-Object System.Collections.Generic.List[object] $dateDirs = Get-ChildItem -Path $InboxRoot -Directory -ErrorAction SilentlyContinue | Where-Object { $_.Name -match \"^\\d{4}-\\d{2}-\\d{2}$\" } foreach ($dateDir in $dateDirs) { $candidateFiles = @() # 1. 日期目录下直接的 Markdown 文件。 $candidateFiles += Get-ChildItem ` -Path $dateDir.FullName ` -Filter \"*.md\" ` -File ` -ErrorAction SilentlyContinue # 2. raw-pages 子目录下的 Markdown 文件。 $rawDir = Join-Path $dateDir.FullName \"raw-pages\" if (Test-Path $rawDir -PathType Container) { $candidateFiles += Get-ChildItem ` -Path $rawDir ` -Filter \"*.md\" ` -File ` -ErrorAction SilentlyContinue } foreach ($file in @($candidateFiles | Sort-Object FullName -Unique)) { try { $hash = Get-FileSha256 -Path $file.FullName $stateKey = $hash if ($State.ContainsKey($stateKey)) { continue } $files.Add([pscustomobject]@{ FullName = $file.FullName Name = $file.Name Hash = $hash Date = $dateDir.Name }) } catch { Write-LearnLog \"扫描文件失败：$($file.FullName)；原因：$($_.Exception.Message)\" \"WARN\" } } } return @($files)}"
}
