{
  "timestamp": "2026-06-29T21:24:52.4936858+08:00",
  "response": "self-loop.ps1\n行号问题类型描述修复代码8-11 / 33 / 80 / 85protocol硬编码 C:\\Users\\... 和多处 D:\\Code\\...，没有从 CLAUDE_PROJECT_DIR / AI_ROOT 推导路径，违反项目路径协议。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$OllamaBin = if ($env:OLLAMA_BIN) { $env:OLLAMA_BIN } else { \"ollama\" }\n$Model = \"qwen2.5-coder:7b\"\n$DataDir = Join-Path $RootDir \"ai-infra\\data\"\n$TopicsFile = Join-Path $DataDir \"learning-queue.txt\"\n$LearningLog = Join-Path $DataDir \"learning-log.jsonl\"\n$GptStatusPath = Join-Path $RootDir \"ai-pipeline\\gpt-queue\\server-status.json\"\n$GptAsk = Join-Path $RootDir \"ai-infra\\scripts\\gpt-ask.ps1\"\n$MemoryDir = Join-Path $RootDir \"memory\"\nNew-Item -ItemType Directory -Force -Path $DataDir | Out-Null\nNew-Item -ItemType Directory -Force -Path $MemoryDir | Out-Null</code></pre> |\n| 33 | bug | server-status.json 不存在或 JSON 损坏时，(... | ConvertFrom-Json).ready 可能报错；当前不在 try 内，会污染自驱循环状态判断。 | <pre><code>$gptReady = $false\ntry {\nif (Test-Path -LiteralPath $GptStatusPath) {\n$status = Get-Content -LiteralPath $GptStatusPath -Raw -Encoding UTF8 | ConvertFrom-Json\nif ($status.PSObject.Properties.Name -contains \"ready\") {\n$gptReady = ($status.ready -eq $true)\n}\n}\n} catch {\n$gptReady = $false\n}</code></pre> |\n| 42 / 50-51 | bug | Get-Content 返回单行时 $queue 会变成字符串，$queue[0] 取到的是第一个字符，不是第一个主题；1..0 在单主题时也会产生错误剩余队列。 | <pre><code>$queue = @(Get-Content -LiteralPath $TopicsFile -Encoding UTF8 | Where-Object {\n$_ -notmatch '^\\s*#' -and $_ -notmatch '^\\s*$'\n})\nif ($queue.Count -eq 0) {\n$defaultTopics | Set-Content -LiteralPath $TopicsFile -Encoding UTF8\n$queue = @($defaultTopics)\n}\n$topic = [string]$queue[0]\n$remaining = if ($queue.Count -gt 1) { @($queue[1..($queue.Count - 1)]) } else { @() }\nSet-Content -LiteralPath $TopicsFile -Value $remaining -Encoding UTF8</code></pre> |\n| 67-71 | bug | Ollama 调用失败时没有检查 $LASTEXITCODE，会把错误输出当学习结果写入日志并继续送 GPT/存档。 | <pre><code>$result = $prompt | & $OllamaBin run $Model 2>&1\n$ollamaOk = ($LASTEXITCODE -eq 0)\n$result = $result -replace '\\x1b[[0-9;]*[a-zA-Z]', ''\nif (-not $ollamaOk -or [string]::IsNullOrWhiteSpace(($result -join \"n\"))) { Write-Host \"Ollama学习提取失败，停止本轮自驱任务。\" exit 1 }</code></pre> | | 85 | protocol | 存档路径写到 C:\\Users...\\memory`，同时没有确保目录存在；违反项目内存目录协议。 | <pre><code>$slug = $topic -replace '[^\\w]+','-' -replace '-+','-'\n$file = Join-Path $MemoryDir \"learn-$slug.md\"</code></pre> |\n\nblocker-pack.ps1\n行号问题类型描述修复代码5protocol阻塞包路径硬编码 D:\\Code，没有跟随项目根。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$dir = Join-Path $RootDir \".ai-state\\blockers$ts\"</code></pre> |\n| 8-27 | protocol | 该包明确用于“问 GPT”，但 $Goal / $Attempts 未脱敏，可能把 token、key、IP、cookie 之类直接外发。 | <pre><code>function Redact-Text {\nparam([string]$Text)\nif ([string]::IsNullOrWhiteSpace($Text)) { return $Text }\n$Text = $Text -replace '(?i)(password|passwd|pwd|token|secret|api[-]?key|authorization|cookie)\\s*[=:]\\s*[^\\s,;}]+', '$1=[REDACTED]'\n$Text = $Text -replace '(?i)\\bBearer\\s+[A-Za-z0-9.~+/=-]+', 'Bearer [REDACTED]'\n$Text = $Text -replace '([A-Za-z0-9_-]{40,})', '[TOKEN_REDACTED]'\n$Text = $Text -replace '\\b(?:\\d{1,3}.){3}\\d{1,3}\\b', '[IP_REDACTED]'\nreturn $Text\n}\n$safeGoal = Redact-Text $Goal\n$safeAttempts = Redact-Text $Attempts\nhere-string 里使用 $safeGoal / $safeAttempts\n# Blocker Pack: $safeGoal\n$safeGoal\n$safeAttempts</code></pre> |\n\nboss-update.ps1\n行号问题类型描述修复代码4 / 22bug输出路径硬编码 D:\\Code\\.ai-state\\boss-update.md，且没有确保 .ai-state 目录存在；目录不存在时 Out-File 会失败。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$stateDir = Join-Path $RootDir \".ai-state\"\nNew-Item -ItemType Directory -Force -Path $stateDir | Out-Null\n$file = Join-Path $stateDir \"boss-update.md\"</code></pre> |\n\nexplain-failure.ps1\n行号问题类型描述修复代码8 / 125protocolFailuresDir 和 lessons-search.ps1 路径硬编码 D:\\Code，不跟随项目根。<pre><code>$RootDir = if ($env:CLAUDE_PROJECT_DIR) {\n[IO.Path]::GetFullPath($env:CLAUDE_PROJECT_DIR)\n} elseif ($env:AI_ROOT) {\n[IO.Path]::GetFullPath($env:AI_ROOT)\n} else {\n\"D:\\Code\"\n}\n$FailuresDir = Join-Path $RootDir \"ai-infra\\failures\"\n$LessonsSearch = Join-Path $RootDir \"ai-infra\\scripts\\lessons-search.ps1\"\n后面替换：\n$lessonResult = & pwsh -File $LessonsSearch -Task $query 2>&1 | Select-String \"得分:|命中:\"</code></pre> |\n| 39 | bug | Substring 长度用的是 (Get-Item $errorFile).Length 文件字节数，不是字符串长度；包含中文/UTF8 时字节数可能大于字符数，导致 substring 越界。 | <pre><code>$errorText = Get-Content -LiteralPath $errorFile -Raw -Encoding UTF8\n$maxLen = [Math]::Min(500, $errorText.Length)\nWrite-Host ($errorText.Substring(0, $maxLen))</code></pre> |\n| 91-111 | bug | 如果 manifest.json 缺失或损坏，$manifest 为 $null，诊断分支仍访问 $manifest.error_type / $manifest.summary，会产生空诊断并影响 lessons 查询。 | <pre><code>$errorType = \"unknown\"\n$summary = \"\"\nif ($null -ne $manifest) {\nif ($manifest.PSObject.Properties.Name -contains \"error_type\") { $errorType = [string]$manifest.error_type }\nif ($manifest.PSObject.Properties.Name -contains \"summary\") { $summary = [string]$manifest.summary }\n}\n后续把 $manifest.error_type 替换为 $errorType\n把 $manifest.summary 替换为 $summary</code></pre> |"
}
