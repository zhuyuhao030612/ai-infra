{
  "timestamp": "2026-06-29T19:26:33.2646842+08:00",
  "response": "PowerShell# D:\\Code\\maintenance-start.ps1Set-StrictMode -Version 2.0$ErrorActionPreference = \"Continue\"function Get-ProjectRoot { if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { return (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } $current = $PSScriptRoot for ($i = 0; $i -lt 8; $i++) { if (-not $current) { break } if ( (Test-Path -LiteralPath (Join-Path $current \"CLAUDE.md\")) -or (Test-Path -LiteralPath (Join-Path $current \"CAPABILITY_REGISTRY.md\")) -or (Test-Path -LiteralPath (Join-Path $current \".claude\\run-state\\run-state.lib.ps1\")) ) { return $current } $parent = Split-Path -Parent $current if (-not $parent -or $parent -eq $current) { break } $current = $parent } return $PSScriptRoot}function Set-StateProp { param( [Parameter(Mandatory = $true)][object]$State, [Parameter(Mandatory = $true)][string]$Name, [object]$Value ) if ($State.PSObject.Properties.Name -contains $Name) { $State.$Name = $Value } else { $State | Add-Member -NotePropertyName $Name -NotePropertyValue $Value }}$root = Get-ProjectRoot$env:CLAUDE_PROJECT_DIR = $root$runStateLib = Join-Path $root \".claude\\run-state\\run-state.lib.ps1\"if (Test-Path -LiteralPath $runStateLib) { try { . $runStateLib } catch { }}if (-not (Get-Command Get-RunStatePath -ErrorAction SilentlyContinue)) { function Get-RunStatePath { return (Join-Path $root \".claude\\run-state\\current.json\") }}if (-not (Get-Command Save-RunState -ErrorAction SilentlyContinue)) { function Save-RunState { param([Parameter(Mandatory = $true)][object]$State) $path = Get-RunStatePath $dir = Split-Path -Parent $path if (-not (Test-Path -LiteralPath $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null } $State | ConvertTo-Json -Depth 20 | Set-Content -LiteralPath $path -Encoding UTF8 }}if (-not (Get-Command Read-RunState -ErrorAction SilentlyContinue)) { function Read-RunState { $path = Get-RunStatePath if (-not (Test-Path -LiteralPath $path)) { return $null } try { return (Get-Content -LiteralPath $path -Raw -Encoding UTF8 | ConvertFrom-Json) } catch { return $null } }}$statePath = Get-RunStatePath$stateDir = Split-Path -Parent $statePath$allowPath = Join-Path $stateDir \"allow-maintenance.once\"$baselineDir = Join-Path $root \".claude\\baselines\\pre-maintenance\"$hookPaths = @( (Join-Path $root \".claude\\hooks\\stop-gate.ps1\"), (Join-Path $root \".claude\\hooks\\posttool-recorder.ps1\"), (Join-Path $root \".claude\\hooks\\block-dangerous.ps1\"))try { if (-not (Test-Path -LiteralPath $stateDir)) { New-Item -ItemType Directory -Path $stateDir -Force | Out-Null } if (-not (Test-Path -LiteralPath $baselineDir)) { New-Item -ItemType Directory -Path $baselineDir -Force | Out-Null } \"maintenance_mode=true; created_at=$(Get-Date -Format s)\" | Set-Content -LiteralPath $allowPath -Encoding UTF8 foreach ($hookPath in $hookPaths) { if (Test-Path -LiteralPath $hookPath) { Copy-Item -LiteralPath $hookPath -Destination (Join-Path $baselineDir (Split-Path -Leaf $hookPath)) -Force } else { Write-Host \"WARN: hook missing, cannot backup: $hookPath\" } } $state = Read-RunState if ($null -eq $state) { $state = [pscustomobject]@{} } Set-StateProp -State $state -Name \"maintenance_mode\" -Value $true Set-StateProp -State $state -Name \"maintenance_started_at\" -Value (Get-Date).ToString(\"s\") Set-StateProp -State $state -Name \"maintenance_allow_path\" -Value $allowPath Save-RunState -State $state Write-Host \"维护模式已开启。改完Hook后跑 maintenance-close.ps1\" exit 0}catch { Write-Host (\"维护模式开启失败: \" + $_.Exception.Message) exit 1}\nPowerShell# D:\\Code\\maintenance-close.ps1Set-StrictMode -Version 2.0$ErrorActionPreference = \"Continue\"function Get-ProjectRoot { if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { return (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } $current = $PSScriptRoot for ($i = 0; $i -lt 8; $i++) { if (-not $current) { break } if ( (Test-Path -LiteralPath (Join-Path $current \"CLAUDE.md\")) -or (Test-Path -LiteralPath (Join-Path $current \"CAPABILITY_REGISTRY.md\")) -or (Test-Path -LiteralPath (Join-Path $current \".claude\\run-state\\run-state.lib.ps1\")) ) { return $current } $parent = Split-Path -Parent $current if (-not $parent -or $parent -eq $current) { break } $current = $parent } return $PSScriptRoot}function Set-StateProp { param( [Parameter(Mandatory = $true)][object]$State, [Parameter(Mandatory = $true)][string]$Name, [object]$Value ) if ($State.PSObject.Properties.Name -contains $Name) { $State.$Name = $Value } else { $State | Add-Member -NotePropertyName $Name -NotePropertyValue $Value }}function Invoke-Step { param( [Parameter(Mandatory = $true)][string]$Name, [Parameter(Mandatory = $true)][scriptblock]$Action ) $startedAt = Get-Date $output = @() $exitCode = 0 try { $output = @(& $Action 2>&1 | ForEach-Object { [string]$_ }) $exitCode = $LASTEXITCODE if ($null -eq $exitCode) { $exitCode = 0 } } catch { $output = @($_.Exception.Message) $exitCode = 1 } $ok = ($exitCode -eq 0) [pscustomobject]@{ name = $Name ok = $ok exit_code = $exitCode started_at = $startedAt.ToString(\"s\") finished_at = (Get-Date).ToString(\"s\") output = $output }}$root = Get-ProjectRoot$env:CLAUDE_PROJECT_DIR = $root$runStateLib = Join-Path $root \".claude\\run-state\\run-state.lib.ps1\"if (Test-Path -LiteralPath $runStateLib) { try { . $runStateLib } catch { }}if (-not (Get-Command Get-RunStatePath -ErrorAction SilentlyContinue)) { function Get-RunStatePath { return (Join-Path $root \".claude\\run-state\\current.json\") }}if (-not (Get-Command Save-RunState -ErrorAction SilentlyContinue)) { function Save-RunState { param([Parameter(Mandatory = $true)][object]$State) $path = Get-RunStatePath $dir = Split-Path -Parent $path if (-not (Test-Path -LiteralPath $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null } $State | ConvertTo-Json -Depth 20 | Set-Content -LiteralPath $path -Encoding UTF8 }}if (-not (Get-Command Read-RunState -ErrorAction SilentlyContinue)) { function Read-RunState { $path = Get-RunStatePath if (-not (Test-Path -LiteralPath $path)) { return $null } try { return (Get-Content -LiteralPath $path -Raw -Encoding UTF8 | ConvertFrom-Json) } catch { return $null } }}$statePath = Get-RunStatePath$stateDir = Split-Path -Parent $statePath$allowPath = Join-Path $stateDir \"allow-maintenance.once\"$psGuard = Join-Path $root \"ai-infra\\scripts\\ps-guard.ps1\"$hookPaths = @( (Join-Path $root \".claude\\hooks\\stop-gate.ps1\"), (Join-Path $root \".claude\\hooks\\posttool-recorder.ps1\"), (Join-Path $root \".claude\\hooks\\block-dangerous.ps1\"))$results = @()foreach ($hookPath in $hookPaths) { $hookName = Split-Path -Leaf $hookPath $results += Invoke-Step -Name \"ps-guard $hookName\" -Action { if (-not (Test-Path -LiteralPath $psGuard)) { Write-Host \"ps-guard missing: $psGuard\" exit 1 } if (-not (Test-Path -LiteralPath $hookPath)) { Write-Host \"hook missing: $hookPath\" exit 1 } & $psGuard $hookPath }}$automationLoop = Join-Path $root \"test-automation-loop.ps1\"$negativeGates = Join-Path $root \"test-negative-gates.ps1\"$results += Invoke-Step -Name \"test-automation-loop\" -Action { if (-not (Test-Path -LiteralPath $automationLoop)) { Write-Host \"missing: $automationLoop\" exit 1 } pwsh -NoProfile -ExecutionPolicy Bypass -File $automationLoop}$results += Invoke-Step -Name \"test-negative-gates\" -Action { if (-not (Test-Path -LiteralPath $negativeGates)) { Write-Host \"missing: $negativeGates\" exit 1 } pwsh -NoProfile -ExecutionPolicy Bypass -File $negativeGates}$failed = @($results | Where-Object { -not $_.ok })$reportPath = Join-Path $stateDir \"maintenance-close-report.json\"try { if (-not (Test-Path -LiteralPath $stateDir)) { New-Item -ItemType Directory -Path $stateDir -Force | Out-Null } [pscustomobject]@{ ok = ($failed.Count -eq 0) generated_at = (Get-Date).ToString(\"s\") results = $results } | ConvertTo-Json -Depth 30 | Set-Content -LiteralPath $reportPath -Encoding UTF8}catch { Write-Host (\"WARN: 写维护报告失败: \" + $_.Exception.Message)}if ($failed.Count -gt 0) { Write-Host \"维护模式关闭失败，保留维护模式。失败详情：\" foreach ($item in $failed) { Write-Host (\"- {0}: exit={1}\" -f $item.name, $item.exit_code) if ($item.output) { Write-Host (($item.output | Select-Object -First 20) -join \"`n\") } } Write-Host \"维护模式仍开启。修复后重新运行 maintenance-close.ps1\" exit 1}try { if (Test-Path -LiteralPath $allowPath) { Remove-Item -LiteralPath $allowPath -Force -ErrorAction SilentlyContinue } $state = Read-RunState if ($null -eq $state) { $state = [pscustomobject]@{} } Set-StateProp -State $state -Name \"maintenance_mode\" -Value $false Set-StateProp -State $state -Name \"maintenance_closed_at\" -Value (Get-Date).ToString(\"s\") Save-RunState -State $state Write-Host \"维护模式已关闭, A+\" exit 0}catch { Write-Host (\"维护验证已通过，但关闭维护模式失败: \" + $_.Exception.Message) exit 1}\nPowerShell# D:\\Code\\.claude\\hooks\\block-dangerous.ps1# 修改: 新增维护模式 helper，并在 Test-SensitivePath / Test-AiStartGate 中调用# 只放宽 .claude\\hooks\\*.ps1 的编辑和流程门禁；危险命令、.env/.pem/.ssh、敏感内容仍按原规则拦截。function Get-MaintenanceAllowPath { try { if (Get-Command Get-RunStatePath -ErrorAction SilentlyContinue) { return (Join-Path (Split-Path -Parent (Get-RunStatePath)) \"allow-maintenance.once\") } $root = if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } else { Split-Path -Parent (Split-Path -Parent $PSScriptRoot) } return (Join-Path $root \".claude\\run-state\\allow-maintenance.once\") } catch { return \"\" }}function Test-MaintenanceMode { try { $allowPath = Get-MaintenanceAllowPath return (-not [string]::IsNullOrWhiteSpace($allowPath) -and (Test-Path -LiteralPath $allowPath)) } catch { return $false }}function Test-HookPs1Path { param([string]$Path) try { if ([string]::IsNullOrWhiteSpace($Path)) { return $false } $root = if ($env:CLAUDE_PROJECT_DIR -and (Test-Path -LiteralPath $env:CLAUDE_PROJECT_DIR)) { (Resolve-Path -LiteralPath $env:CLAUDE_PROJECT_DIR).Path } else { Split-Path -Parent (Split-Path -Parent $PSScriptRoot) } $full = $Path if (-not [System.IO.Path]::IsPathRooted($full)) { $full = Join-Path $root $full } $normalized = [System.IO.Path]::GetFullPath($full) $hookDir = [System.IO.Path]::GetFullPath((Join-Path $root \".claude\\hooks\")) return ( $normalized.StartsWith($hookDir, [System.StringComparison]::OrdinalIgnoreCase) -and $normalized.EndsWith(\".ps1\", [System.StringComparison]::OrdinalIgnoreCase) ) } catch { return $false }}function Test-MaintenanceAllowsHookEdit { param([string]$Path) return ((Test-MaintenanceMode) -and (Test-HookPs1Path -Path $Path))}# 在现有 Test-SensitivePath 里，对 hooks 路径拦截前加入：# if (Test-MaintenanceAllowsHookEdit -Path $Path) {# return $false# }## 注意：这行必须只放在 .claude\\hooks\\*.ps1 相关判断前，# 不要放在 .env/.pem/.ssh/settings.json 等敏感路径总判断前。# 示例改法：# if ($Path -match \"\\\\\\.claude\\\\hooks\\\\.*\\.ps1$\") {# if (Test-MaintenanceAllowsHookEdit -Path $Path) {# return $false# }# return $true# }# 在现有 Test-AiStartGate 或 Invoke-AiStartRequiredForFileMutation 开头加入：# if (Test-MaintenanceMode) {# return $false# }## 这只放宽 ai-start/task_started 流程门禁；# 不要绕过危险命令、敏感内容、.env/.pem/.ssh 检查。"
}
